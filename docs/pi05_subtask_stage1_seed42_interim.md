首个种子的等预算动作对照已经完成。生成 subtask 的 G 模型能输出有意义的阶段描述，但目前没有足够证据说明它稳定优于不使用 subtask 的 B1：完整动作块误差略低，短执行窗口误差略高，五项主要误差差值的配对区间均包含 0。

这是验证集上的阶段性记录；O 对照已完成，种子 43 已完成 M1/M2 并接续后续训练，种子 44 仍在队列中，最终测试尚未开始。当前结论不改变既定训练预算、模型选择或统一执行长度规则。

G 与 B1 共享同一个经过数据适配的官方 π₀.₅ checkpoint C0，均冻结 backbone 并完成 4,000 次 action 更新、有效 batch 32、相同学习率阶段与自然样本分布。G 另有独立的 subtask Transformer 和 CE 训练；因此这里只匹配 action 更新预算，不声称总计算量相同。B1 输入不含 subtask。独立训练的 O 使用 GT subtask，仅作为离线对照。其 H50 joint/gripper RMSE 为 0.13529330/0.01814635，前10有效帧为0.05424863/0.01239633。G−O的H50 joint差值为0.00568644，按轨迹配对95%区间[0.00239752,0.00884741]；该首轮结果显示G尚未达到真实条件训练的动作表现，其他指标与后续种子仍需完整报告。

动作比较使用同一验证集的 128 个起点、每点 2 个固定噪声，共 20 条 episode。表内点估计从已保存的 50×14 原单位预测重新计算；噪声先按帧平均，帧均方误差再开方。区间以任务分层、对整条 episode 做 2,000 次配对 bootstrap，固定随机种子 90210。区间条件于本次两个训练好的模型，不覆盖所有训练随机性。

| 指标（越低越好） | G | B1 | G 相对 B1 | G−B1 的 95% 配对区间 |
|---|---:|---:|---:|---:|
| 归一化 native14 flow MSE | 0.03237188 | 0.03260968 | -0.73% | [-0.001557, +0.001048] |
| 50 步关节 RMSE | 0.14097974 | 0.14211989 | -0.80% | [-0.004866, +0.002065] |
| 50 步夹爪 RMSE | 0.01862871 | 0.01881906 | -1.01% | [-0.000711, +0.000449] |
| 前 10 有效帧关节 RMSE | 0.05473419 | 0.05455012 | +0.34% | [-0.000954, +0.001182] |
| 前 10 有效帧夹爪 RMSE | 0.01223723 | 0.01216213 | +0.62% | [-0.001031, +0.001197] |

前 10 帧只是原计划中的固定诊断窗口；最终执行长度尚未确定，将仅根据全部验证的完整策略 p95 延迟加 20% 余量，在 10/15/20/25/50 帧中选取，再统一应用于所有方法。不能根据这张误差表挑选对 G 最有利的执行长度。

分组描述显示收益并不一致。50 步关节误差的下降主要出现在 eggplant：G/B1 为 0.12687045/0.12971600；sweet potato 为 0.15342158/0.15318661。跨阶段窗口的关节误差略有改善，但其前 10 帧夹爪误差略高。这里的跨阶段标记由原 50 帧动作窗口定义；这些是探索性分组结果，未做多重比较校正，不能挑选有利子组当作总体结论。

C0 的 50 步关节 RMSE 为 0.15428421。G 相比 C0 的明显下降不能全部归因于 subtask，因为 B1 在相同额外动作训练后也降到了 0.14211989；公平的增量效应主要看 G 与 B1，并由三种子验证及最终测试确认。

完整自然验证集有 13,515 帧。最终 G 的 subtask exact match 为 85.36%，macro-F1 为 79.23%；M1 预热后的对应结果为 83.83%/78.52%。只使用全局任务和已过时间的训练期中位阶段起点诊断为 71.72%/59.66%。将三路图像置黑、保留任务/state 后，G 为 20.58%/6.98%。这些结果支持模型使用了当前观测信息，但置黑属于分布外输入干预，不等于重新训练无图像模型，也不证明开放任务规划能力。

边界仍是主要问题。140 个真值切换中，G 匹配 137 个、漏掉 3 个；±10 帧内比例为 49.29%，匹配切换的绝对误差 p50/p95 为 10/23 帧，平均延迟约 4.86 帧。G 每条轨迹有 13.15 次未匹配变化，高于 M1 的 10.5 次。边界附近 exact match 为 57.48%，远处为 93.12%。切换定位改善和额外抖动增加同时存在，阶段跟踪尚不稳定。

同一 G 权重下，生成/置空/同任务打乱/GT 条件的 50 步关节 RMSE 分别为 0.14097974/0.14223922/0.14640432/0.13855609。文本条件确实改变动作，打乱条件会使误差增加；其中 GT 干预不是独立训练的 O，不能代替 O 的实验结果。

完整策略计时各 128 次、每张 GPU 先 warmup 3 次。G p50/p95 为 359.55/386.98 ms，B1 为 301.95/340.79 ms，C0 为 306.75/351.34 ms。计时包括转换/tokenizer、视觉编码、prefix、生成及 flow 采样和原单位输出，不包括视频解码、传感器传输或机械臂控制。单策略独立加载后的峰值显存测量仍在后续队列中；这些时间不是闭环成功率或真机实时性证明。

实现约束已落实：生成的文本进入动作 prompt；S 仅由 CE 更新、A 仅由 flow loss 更新，B 冻结；两组 optimizer 的参数不重叠。真实八卡恢复及权重冻结检查已通过。G 最后 875 次 action 更新使用预测或条件置空，占总 4,000 次更新的 21.875%，不含 GT 动作条件。

O 的4,000次动作更新及离线评估已完成；当前按固定协议顺序运行种子43/44的M1/M2/M3/B1/O。全部验证完成后，测量正常部署策略、固定测试清单和统一执行长度，再运行最终测试并报告三种子均值、标准差与配对区间。物理机器人闭环不在已经完成的验收中。

复现依据：`logs/pi05_subtask_stage1/g_b1_seed42_validation_pair.json` 保存完整配对统计与来源哈希；`matched_validation_seed42_complete_b1/curves.json` 保存匹配 action 更新数的完整曲线；各模型的 `report.json` 和 `native_rank_*.npz` 保留原始评估与动作数组。新增分析脚本为 `scripts/analyze_subtask_pilot_pair.py`，只读取验证产物，不修改模型或训练。

权重身份：

- G：`checkpoints/pi05_piper_stage1/m3_pilot_seed42/step_003500`，SHA256 `64600bed0b60e0529ac2721edb55717ac1c80fba2a607292857668a35e7a9275`。
- B1：`checkpoints/pi05_piper_stage1/b1_pilot_seed42/step_004000`，SHA256 `b99c6cc09a449d824b3cb048d97df34dfd7fbaceb43d347b77c113d4d684163f`。
