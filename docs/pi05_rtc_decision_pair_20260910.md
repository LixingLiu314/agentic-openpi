# RTC 与决策/定位实验接续

2026-09-10。用户已确认两组：①现有数据选臂/选物对比＋前25动作强化；②①＋S轻量目标定位，A结构不改。新增learnable query议题仅讨论，不替换已确认实验。

## Agilex RTC

已安装默认开启、可关闭的RTC，GUI“连续执行（RTC）”，默认更新间隔25步。维持30Hz执行旧动作尾段，同时生成下一段，按实际已经执行的步数跳过新预测中过期部分。采用软掩码端点VJP约束衔接；关闭恢复同步模式。服务端按连接保存上次预测/递归S状态，客户端不能任意提供动作前缀。异常、超时、动作耗尽和控制节奏失常停止，不追赶发送动作。

实际安装记录：Agilex logs/rtc_deployment_20260910。原8001服务已用同一reach-arm step005000重载；未操作机器人、驱动或Dataarm。重开GUI加载新的控件。

通过：5项采样数学检查、7项线程调度、94项客户端回归；真实checkpoint CPU RTC与原始首请求完全一致，独立会话和归一化往返通过；真实WebSocket＋模拟执行器65步/3请求，最大间隔33.56毫秒。真实GPU模型服务后续RTC请求309–324毫秒，低于25步833毫秒。均为无运动验证，不是物理闭环成功证明。

未来schema9两组的catalog、GUI显示、原生loader、服务路由已安装，27项识别/握手回归通过。当前模型仍是原reach-arm step005000；两组新权重尚不存在。

## 实验配方

共同官方fresh B/A、seed42全新共有S、limited末两层LoRA16/32、4×512递归记忆、unroll4、5000 optimizer updates、global256/8卡micro32/accum1/workers4、warmup500/peak2.5e-5/end2.5e-6、save/eval500。沿用178train/20val/no-test与train-only norm；最终step005000优先。

- decision_prefix：按任务×审核布局×开盖臂均衡轨迹起点，从frame0开始，保留0.5–1秒因果顺序和跨更新状态；采样包含物体reach起点。初始45帧与物体reach前30帧的S样本权重2，关键差异token权重4。Action前25步权重2、后25步权重1，并除平均权重1.5。全32原始flow/普通CE单独记录。
- 两类动作ranking分别最多2对/rank，系数0.1、margin0.01，native14前25步。只在S正确生成、未dropout、训练标签在过去7/未来24帧稳定时启用。同物换臂保持任务；同臂换物同时修改全局任务及subtask，且均来自现有训练词表。使用共同噪声/时间/真实动作，不伪造反事实动作示范。分别记录两类覆盖数和hinge。
- decision_grounded：在第一组上增加S专用目标热图分支。任务/状态文字特征作为query，对高相机16×16图像patch打分；预测目标特征和二维位置作为额外一个S context token。A仍只接收普通生成文字。定位目标不作为policy输入。
- 定位辅助每rank/update额外2张已审核训练帧，合计16辅助图像，系数0.05，无额外optimizer update；这部分额外监督/计算预算明确记录。训练32段64帧117个可见物体点；验证独立8段16帧30点。色彩/轮廓仅帮助标注，80帧视觉审核及与精确缓存像素逐一对照；遮挡不猜测。两物均可见时可切换相应任务监督位置，只用于定位分支，不配伪造A动作。

定位CE和subtask CE只到S；action/ranking只到A及allowed B。Grounding使用当前B输出的detached特征；不更新官方vision tower。共有S初始化一致，第二组新增head独立种子4201。新模型schema9 / official_pi05_recurrent_decision_v1。

## 队列与检查

新控制logs/rtc_grounding_pair_20260910。旧semantic_s继续；仅旧managed scheduler被暂停，未启动semantic_s_actionrank取消。新队列通过pidfd等待当前torchrun退出，不轮询训练进度。退出后接管旧第一组最终5000/native/诊断，按精确进程身份退役已暂停旧调度器，再进行新两组八卡4→8恢复和native/梯度工程检查，通过后逐组正式训练。不要恢复旧调度器或重复派发。

CPU已通过：反事实条件/gating、前缀权重梯度、S-only定位梯度/共有初始化/因果性、真实资产与采样恢复/8初始strata/117训练点/原生batch。八卡工程仍待当前训练结束，不能将排队解释为已通过。

标准验证保留；最终增加前15/25/50步选臂统计、同观测换目标、actor文字干预、连续/每帧重置对比，第二组另报30验证定位点误差。代理动作支配臂/热图命中率不等于真机抓物成功。

新W&B视图：https://wandb.ai/xiahy23-tsinghua-university/agentic-openpi-pi05-subtask?nw=qfbemtvwl95 。只在真正训练后出现曲线；首10步由训练器触发一次远端/本地指标及示例检查，无持续进度查询。视图第一次自动审批因未找到外部发布授权被拒；核实2026-09-08原始用户“之后都按照规范配置wandb的显示”和服务器AGENTS.md 350–356后，带证据重试已批准、完成、读回验证。无未决审批。

## ETA依据

为此次替换队列于北京时间22:00做一次有界读取：当前semantic_s完成2022/5000，近20步平均6.974秒/步，明显慢于早先工程3.71秒。保留5000步预算。按当前速度及额外分支粗估：旧第一组9月11日04:15左右；新第一组9月11日16–18时；两组全部9月12日04–06时，均含估计的验证/存盘余量。新组合尚无真实八卡耗时，后续按工程测量/实际ETA一次重估。此记录不据此查询GPU占用或干预其他任务。
