# agentic-openpi server development

## N1修复已提交；重新派发因外部容量阻塞退出（2026-09-12 10:41北京时间）

- 原teacher logits问题已修复并实际通过完整官方尺寸模型的原生/因果/梯度检查；实现commit e005ecfa857670e9a45e11070cc1c9991acebb99。不是简单放宽阈值，完整teacher/Action最大差均0。旧工程checkpoint用于定位和修复验证，不作为formal初始化或新八卡恢复证据。
- 新root logs/pi05_native_n1_20260912/attempt_01于10:33:39真实派发，依次测试8卡micro32×accum1、16×2、8×4（均global256），三档均在首次optimizer update完成前CUDA OOM，10:40:05有限流程exit1。capacity_terminal.json于10:41核验launcher及3个torchrun身份均已退出、145份计算源码不变；没有新4→8恢复成功记录、没有formal输出或startup10。phase.json仍是最后工程阶段的历史值，不代表仍在运行。不得重复派发同一root或把这次容量失败误读为数值修复无效。
- OOM原始异常列出每卡外部占用约56.07GiB；仅按日志PID做CPU身份核对，未查询GPU利用率/占用API。确认8个375186–375193/created1789180324.93来自ws_liyan/RLinf的toolkits/gpu_hold.py --memory-fraction0.70，父268393/created1789178761.73为run_robotwin_hybrid_optimization_a800.sh。此PID只作历史证据，任何操作前重验真实身份。它们不属于我们的gpu_reservation workers，不得擅自停止/修改或扩大接管授权。
- 依照已有共存规则，把真实外部pipeline身份登记为过期随进程退出的concurrent lease，避免我们的guard在N1退出后与该外部任务争用。证据logs/pi05_native_n1_repair_20260912/external_pipeline_lease.json；外部任务/guard没有收到信号。该保护登记不等于停止/控制它的授权。
- 当前需要用户协调显存或明确授权处理上述外部保留任务，不能假称正式训练已重启或给无依据ETA。没有新增自动重试/等待队列；唯一vla-action-stop提醒保持PAUSED，资源问题解决且真实formal首10步通过后再估ETA并启用。同left/right arm、official fresh、B/A梯度与5000/global256合同保持；Q1未派发。

## N1失败修复及重新训练授权（2026-09-12，优先于下方历史排队状态）

- 用户明确要求“解决上述问题后重新开始训练”。只修复并重启N1，不启动尚未确认接口的Q1或其他历史队列。旧parallel action_stop_seed42_v2已完成5000及最终原生检查；N1旧attempt_02于9月11日18:56在工程8步后的检查退出，正式输出尚未创建。实际进程身份审计确认旧两组7条记录均不再存活，证据logs/pi05_native_n1_repair_20260912/pre_repair.json。旧日志、source_manifest、工程权重和原始源码备份全部保留，不重写旧失败记录。
- 同一工程step_000008/SHA aa57379b3aa3f38de2fa7e3487c349fa7df8aa0bc7efca7beb2980b51d0501c3实测定位：训练投影输入1×16×2048，cached检查先投影1×20×2048再切片。目标隐藏特征和norm后特征逐元素相同；BF16词表投影197/2828672项差异、max0.25，argmax无差异，CE差1.1920929e-7。同形状投影、关闭TF32的FP32词表对照、原生Action均逐元素一致。诊断脚本scripts/diagnose_native_n1_parity.py及diagnostic.json记录结果；这不是OOM、NaN或架构/梯度错误的证据。
- 修复cached teacher-forcing在原生norm/lm_head前选目标行，新增teacher_logits_cached，供验证loss和严格检查共用。训练joint_outputs、训练loss公式、生成token路径、模型参数/梯度合同不变，不放宽原有rtol=0/atol=0一致性门槛。CPU新增batch2/变长目标/投影形状回归，并保留独立HF/逐token/保存恢复测试。
- 派发/CPU预检支持“前驱身份已退出且complete5000+native_gate_passed”，不要求已完成前驱仍存活；失败/缺失终态、PID复用、源码变化及重复root/已存在formal输出均拒绝。保留空CUDA可见性修复。新的root logs/pi05_native_n1_20260912/attempt_01；只有实际dispatch_verified和后续startup_verified可证明派发及正式启动，不根据本段规划宣称已开训。
- 本次重做完整模型梯度/原生门槛，并从官方fresh重跑8卡32×1的4→8工程保存恢复；全部通过后再次官方fresh正式5000/global256/seed42、同left/right arm178/20/train-only norm。正式输出仍checkpoints/pi05_piper_native/n1_action_stop_seed42_v1，不继承任何工程更新。所有GPU任务run_concurrent，其他任务/guard不动；测试和实现先commit，启动后只另提交状态文档。首10步W&B核验与一次有界ETA后更新唯一vla-action-stop完成提醒，不短周期轮询。

## N1之后的输入query实验：本轮规划，未派发（2026-09-11）

- 用户希望“之后再排一个query token的实验”，明确query是直接输入VLM的token，但具体机制尚未想清楚，要求先帮助规划。候选协议docs/pi05_native_query_q1_plan_20260911.md；本轮只新增设计文档和本指导，未实现/派发Q1、未改变当前实验或N1的源码/队列/提醒。此节允许继续讨论query，覆盖历史一概“query暂缓”的设计限制，但不表示接口已经确认或新实验已实际排队。
- 推荐Q1：4×2048可学习输入向量，直接进入既有VLM原生18层；无新Subtask decoder/Q-Former/MLP文本生成器。Q只读普通prefix P和其他Q，不读GT文字；原生文字T读P+Q+因果文字，Action只读原P。P本身不能读Q/T，避免Q→P→A间接泄漏；A位置不增加query长度。仍CE→B+Q、flow→A，B/Q对flow梯度为零，A不读GT/生成文字或query。
- 此Q1是query辅助原生文字生成，不是强制query瓶颈或Action读取query；T保留直读P，所以Q可能被忽略，不能默认形成可解释语义槽。若用户重点是Action直接使用query，必须先明确其前向接口，不可只detach梯度便称Action接口未变。只读设计降低直接前向扰动，不宣称新增query完全无OOD。
- 建议官方fresh B/A+新query，seed42/5000/global256/8卡、同N1最终micro/accum及left/right arm 178/20/train-only norm、单帧unroll1；排在N1后面不等于从N1权重继续训练。只规划这一新增单组，不默认追加query数量扫描、fixed-query训练对照、独立网络、历史、定位或ranking。B+Q同optimizer/联合clip1，A独立；query FP32参数、同LR、独立初始化随机流，须另过容量/保存恢复/因果/梯度/原生cache门槛。
- 当前只可称“Q1候选/待确认计划”，没有Q1等待进程/训练run/耗时实测。N1有效root仍attempt_02，保持其计算源码指纹；以后Q1确认后须独立入口/schema/variant、测试commit、真实依赖N1及最终检查成功的接续收据，不能热改N1等待代码。

## N1实际排队状态（2026-09-11 15:13北京时间）

- 最新有效接续器已派发并读回：logs/pi05_native_n1_20260911/attempt_02/dispatch_verified.json，queued=true、phase=waiting_predecessor，PID1256324/created1789110811.06，完整argv见launcher.process.json。GPU可见性覆盖为null（未继承CPU-only空列表），无N1 GPU工作或正式训练；不要误读attempt_01的历史waiting文件，不要重复派发。
- 运行实现派发SHA3d911c91a1ffad120d766a5476efe9cb8ce871d7，核心N1实现SHA5f3408ab963d7682e95cfb5933aa598a7c6565a6；本次之后仅状态文档提交，不改等待源码指纹。当前前驱仍为attempt_04的formal launcher535442/created1789098587.44，身份与原source_manifest核验通过。正式N1输出n1_action_stop_seed42_v1仍待前驱和八卡门槛成功后自动创建。

## N1排队恢复说明（2026-09-11，优先于下方attempt_01）

- 首次N1等待进程attempt_01/PID1240872确已派发，但最后环境核验发现从CPU-only派发命令继承了空CUDA_VISIBLE_DEVICES；尚无GPU工作/训练。仅该无子进程的N1 waiter在核验PID、创建时间、argv和waiting_predecessor阶段后被SIGTERM，intentional_waiter_stop.json保留原因；当前action_stop_seed42_v2与其他任务未收信号。这不是模型/训练OOM或门槛失败。
- dispatch_native_n1.py现显式清除空的CPU可见性覆盖，保留非空用户映射，并读回真实等待进程的可见性环境；新CPU测试还用实际子进程验证覆盖未被继承。恢复用新root logs/pi05_native_n1_20260911/attempt_02，不复用旧root的waiting状态；正式输出仍checkpoints/pi05_piper_native/n1_action_stop_seed42_v1，因为任何N1训练都尚未开始。实际恢复派发必须看attempt_02/dispatch_verified.json。
- N1模型/训练/数学代码与首次实现commit5f3408a不变，只修改CPU派发环境与预检。attempt_01/source_manifest不重写，所有证据保留；新实现修复提交后再派发attempt_02。W&B仍同一专用视图，不创建重复view/run/提醒。

## N1实施与接续授权（2026-09-11，实际排队收据为准）

- 用户进一步明确“N1排上队”，现已补齐独立N1入口；仅native N1，不恢复旧队列。当前action_stop_seed42_v2原样继续，不改其指纹源码。接续顺序是当前formal launcher成功退出（包含最终原生检查）→N1八卡工程门槛→official fresh正式5000步→最终原生检查。非零退出、源码变化或门槛失败即停止N1接续并保留证据，不把失败当成可自动绕过的条件。
- 新代码scripts/{train_native_n1,run_native_n1,check_native_n1,test_native_n1,preflight_native_n1}.py；模型models_pytorch/native_subtask.py，schema11/official_pi05_native_subtask_n1_v1。无独立Subtask网络或S optimizer，VLM预训练语言层和绑定词表原生生成。原pi05观测prefix保持不变，新增的私有causal文字流使用固定cue `\nSubtask: `；文字读prefix，prefix/Action均看不到文字，A位置编号不增加文字长度。共享同一B层实例，并非新建一套层；仅借用既有parallel_layer纯B/A数学函数，不实例化旧S。
- CPU真实小尺寸PaliGemma/Gemma两层检查通过：原生联合文字前向/逐token cache一致、目标移位/EOS/PAD、未来GT不泄漏、Action对GT不变、文字不修改Action的prefix cache；CE到视觉/投影/语言/词表非零且A零，flow到A非零且B零；绑定参数唯一所有权，保存/双optimizer恢复下一步一致。此证据不能冒充完整官方模型/八卡GPU门槛。完整官方812个存储tensor逐项核对、梯度与原生50×14推理、八卡4→8恢复均由接续器在当前实验结束后实际执行，通过才启动formal。
- 固定left/right arm数据与178train/20val/no-test/train-only norm；CPU契约与cache入口通过，15类实际目标最长11tokens（含EOS），预算16无截断。N1为单帧无递归，unroll1；采样沿用episode streams但无模型历史输入。B/A各自AdamW、clip norm1；5000/global256/seed42/8卡，容量首测32×1，仅实测CUDA OOM可降16×2或8×4。各阶段检查/权重单独留存，正式不继承工程更新。
- 预定权威root logs/pi05_native_n1_20260911/attempt_01，输出checkpoints/pi05_piper_native/n1_action_stop_seed42_v1。仅存在文档/代码不等于排队，必须核验root/launcher.process.json、phase.json和dispatch_verified.json的真实PID/创建时间；等待采用pidfd前驱launcher退出事件，不轮询训练step或GPU。旧前驱身份来自logs/pi05_parallel_action_stop_20260911/attempt_04/formal_launcher.process.json，不复用旧失败decision队列。
- W&B新专用视图https://wandb.ai/xiahy23-tsinghua-university/agentic-openpi-pi05-subtask?nw=2r09j4dx7vn 已通过API读回；等待时没有run或loss数据，不制造空训练记录。formal首10步核验两loss、B/A梯度、三类media，并写startup_verified.json与startup_eta.json。曲线含B/A裁剪前范数和裁剪比例，相对更新仍明确sampled；视觉/投影/语言/词表分项仅工程门槛测量。验证另报native14/all32、前25、关节/夹爪。
- 保留当前唯一vla-action-stop提醒的9月12日01:30检查，不为排队新增重复提醒。届时先核对前驱终态，再看N1实际phase/失败/首10步ETA；如N1在训，按一次有界读取估计其完成时间并更新同一提醒，不将前驱完成误报为N1完成。没有短周期轮询、push、机器人部署/服务切换或动作授权。

## 下一组实验已选定：仅N1，使用left/right arm数据（2026-09-11）

- 用户明确选择“只进行N1，数据记得用left/right arm的那套”。后续仅native_parallel_action_stop；不安排N0、query、显式subtask→Action、Action-limited/full或旧队列。完整协议docs/pi05_native_parallel_n1_reach_arm_20260911.md。本次已完成方案/数据锁定，尚未实现N1或派发训练；方案确认不等于已排队，未来按真实执行收据更新状态。
- N1由VLM原生语言层/词表输出自回归预测subtask，无独立Subtask Transformer decoder；CE→B、flow→A，Action训练/推理均不读GT或生成subtask。首轮单帧无递归记忆；与当前在训实验比较时必须公开记忆与生成路径差异，单N1不能证明语义训练改善动作的因果收益。
- 数据固定Datasets/eggplant_potato_reach_arm_v1、repo ID local/eggplant_potato_reach_arm_v1、assets/pi05_piper_reach_arm_v1/eggplant_potato和.stage1_staging/piper_rgb224_reach_arm_v1。沿用178train/20val/no-test及train-only norm；只三个reach阶段带with the left/right arm，其他阶段不改。本次split/norm/annotations实际SHA256均匹配reach_arm_data契约；不得误用旧无左右臂数据或其他split/norm。
- 官方fresh B/A、seed42、5000/global256/8卡、原LR schedule、B/A独立AdamW及norm1裁剪；须先补齐原生语言接口并通过因果/梯度/原生前向/8卡恢复门槛，提交独立实现后再派发。当前action_stop_seed42_v2继续原样运行，源码指纹不动；当前提醒不因本次方案落地自动修改。此节是最新N1选择，优先于下方历史“其他实验暂缓”的泛指；未发生的实现、工程检查、排队或训练不得声称完成。

## 后续架构硬约束：Subtask 使用 VLM 原生语言生成（2026-09-11）

- 用户明确要求“之后所有都不要出现transformer decoder了”，结合前文指后续方案、代码和新实验不得再新增或沿用独立的 Subtask Transformer decoder（包括当前随机初始化的4层512维S），不能仅更名为其他模块后继续承担同一套独立自回归解码。Subtask 应由 VLM backbone 自身的预训练语言层和原生词表输出路径生成；必要的词表线性投影不等于新增 Transformer decoder。
- 此约束不禁止 VLM 自身已有的 Transformer 结构，也不要求删除原有 Action expert。当前正在运行的 action_stop_seed42_v2 明确保留，不停止、不热改、不重启；旧模型、checkpoint和历史实验文档保留真实记录，不能追溯改写成原生语言生成。当前训练源码指纹与运行身份保持不变。
- 后续讨论先核实 VLM 原生文本输入/输出、移位目标、causal mask、CE及KV-cache生成路径，不再以当前独立S为固定前提扩展query。Query和其他后续实验暂缓；本决定只固化后续架构规则，不授权新增训练、部署、机器人动作或修改现有提醒。
- “由谁生成subtask”和“Action是否读取subtask”是独立决策。本规则不自动恢复S文字回填、S隐藏状态/记忆注入A，也不改变当前CE→B、action→B截断的梯度合同。若保留或重设计历史记忆，须单独明确其接口，不能借此恢复独立S Transformer decoder。
- 论文架构与公开实现须区分：π0.5论文IV-A/B/C描述同一VLM自回归生成subtask并以subtask条件化动作；openpi README明确当前π0.5入口仅支持flow-matching head。公开入口未接好文本生成不等于需要新decoder；应核验checkpoint词表/输出权重及原生生成能力，不能未经实测声称开箱即可正确生成。来源：https://arxiv.org/html/2504.16054v1 和 https://github.com/Physical-Intelligence/openpi 。
- 不再把“subtask作为低层条件”本身判定为OOD或已证实的性能根因。论文监督目标和标注描述支持GT条件/teacher-forcing解读，推理使用模型生成subtask，存在token级和高低层条件级训练—推理差异；论文未披露完整采样替换实现，不能臆称已用scheduled sampling消除gap。当前并行A在训练/推理均不读取subtask，因此没有这条GT→预测subtask的动作条件差异。复用词表不等于复用预训练语言生成能力；独立decoder是否导致性能差仍须证据，不能把架构偏离直接当成已证实因果。

## Git提交习惯（用户明确要求，2026-09-11）

- 阶段性代码、测试、配置和文档修改完成并通过适当检查后，及时在服务器项目仓库创建范围清晰的git commit；进入下一项架构/实验工作前，先提交上一阶段尚未提交的相关修改。不要长期把项目实现全部留在未跟踪文件中。
- 提交前检查git status/diff及暂存清单，按明确路径暂存，排除密钥/凭据、数据集、模型/optimizer权重、运行日志、RGB缓存、临时备份、环境归档和无关用户修改；保留这些文件原地不删除。尊重现有stash和分支，不覆盖或清理用户工作。
- 提交信息写清实际完成内容及必要限制；保存一个历史快照不代表其中每个旧实验均成功，已知失败/未完成门槛如实保留。检查实际commit hash并向用户报告，不能把git add当作已commit。
- 新实验派发前优先提交实现与验证代码，并将commit SHA连同源码哈希纳入运行provenance；运行身份/状态文档可在启动确认后另做小提交。不得为了提交而修改运行中的指纹源码。
- 本授权包括本地git commit，不默认授权push、发布PR、改写历史、amend或强制推送。继续使用仓库已有git身份，非交互SSH使用/media/raid/workspace/surongpeng/anaconda3/bin/git。

## 当前实施授权：并行Action-stop单组（2026-09-11）

- v2正式启动已核验：2026-09-11 11:49派发，11:53首10步W&B/local两loss、S/B/A梯度及三类media对账通过；11:54:56最后一次有界读取17/5000，8个真实rank存活，source_manifest全部不变。训练锁定commit64bc1d9bb2ee72c09b82d009a6983fbd3f2f0db8；后续本状态文档commit不改训练SHA。权威收据attempt_04/startup_handoff.json及正式输出startup_verified.json。v2首步两张实际图已检查，正确标明S文字仅展示。
- v2实测steps3–17平均8.19115秒/update，最后读取时剩余纯训练约11.34小时，加45分钟验证/存盘/最终原生门槛的点估计为9月12日00:00左右；考虑共享负载波动，用户预期约午夜至01:00，单次检查保留01:30缓冲。启动核验已结束，不再主动读取中间steps/checkpoints，除非用户新问进度或到该ETA跟进。不要把早期CE或动作误差当成最终效果。
- 当前恢复身份：logs/pi05_parallel_action_stop_20260911/attempt_04，正式输出checkpoints/pi05_piper_parallel/action_stop_seed42_v2。attempt_03的真实八卡工程及所有原生/梯度门槛已通过；首次formal v1训练10步后，被startup checker继承训练W&B0.19.11的WANDB_SERVICE而使新0.29.0解析失败所中止。不是OOM、NaN或梯度错误；未到500步，无可恢复正式checkpoint，v2必须official fresh重新开始，保留失败v1及全部证据。
- 核验器已清除父进程版本相关的WANDB_SERVICE/_WANDB_SERVICE，不清除凭据；用真实旧SDK live offline parent→新SDK checker完成CPU/network集成，失败v1的step10 loss/梯度/media已真实对账成功，但此晚到验证不表示v1仍在训练。新runner只允许startup-reader和orchestrator两文件改变时复用旧八卡证据，其余训练/模型/data/optimizer/native门槛源必须逐字节相同，并写reused_gate_evidence；这是明确受限复用，不宣称重跑了新一套八卡工程。
- 当前应用不存在旧vla自动化，已创建唯一新heartbeat vla-action-stop：北京时间2026-09-12 01:30单次检查。派发后以本次实际首10步/有界读取重估；后续更新这个新id，不再使用vla，也不要新增重复提醒。若届时未完成，按剩余时间延后同一提醒；完成/失败报告后暂停。
- 用户已明确要求“现在修改训练代码启动新实验”，随后要求先commit并记录提交习惯。历史修改和Git规范已提交为08cb901；并行实现已提交为d855371。以下“本轮只设计/未授权实施”均为较早历史状态，当前只授权这一个新单组，不恢复旧串联队列，也不增开limited/full或其他消融。
- 独立入口scripts/train_parallel_action_stop.py、run_parallel_action_stop.py、check_parallel_action_stop.py及models_pytorch/parallel_subtask.py。schema10 / official_pi05_parallel_multitask_v1，CE→S+B、flow→A；每层A读取的B K/V detach，无S文字回填，S记忆仅在S中。B全量CE更新，不是Frozen。正式fresh official B/A、匹配seed42新S、5000/global256/8卡micro32accum1/unroll4/workers4，现有reach-arm178/20/train-only norm不改。
- attempt_01真实八卡4→8恢复完成，S/A/B均8次更新；原检查错误地用过严逐元素阈值对比另一种BF16联合attention形状，并包含无效PAD位置，随后退出，未启动正式训练。只读同权重诊断表明新路径与原生cached动作逐元素相等；旧joint与cached本身也有相同0.019366最大差。保留attempt_01证据，不把该检查失败称为OOM或模型训练崩溃。模型/训练数学不变；修订checker严格检验native有效prefix/动作逐元素一致，另外关闭TF32做FP32 joint控制，真实分loss反传。
- 完整新版本的工程目录logs/pi05_parallel_action_stop_20260911/attempt_03；4→8保存恢复、原生50×14、session/reset、native有效prefix/action逐元素一致和关闭TF32的FP32 joint检查通过。实际CE梯度S25.906/B2.8728/vision0.2693/language2.8585/A0；action梯度A0.4794/B0/S0。attempt_01/02/03均保留证据，不再派发formal或按旧phase猜测运行中。attempt_03纳入独立parallel首步示例图，明确S文字不条件化A；模型数学未变。
- 当前正式输出checkpoints/pi05_piper_parallel/action_stop_seed42_v2。first10由trainer核验W&B/local loss、S/A/B梯度与输入/输出图及类别表，证据startup_verified.json。规范视图https://wandb.ai/xiahy23-tsinghua-university/agentic-openpi-pi05-subtask?nw=i0indjgwejl 。之后仅一次有界读取估ETA，更新vla-action-stop为ETA单次跟进；不短周期轮询，不查GPU占用。所有GPU工作run_concurrent，保护其他任务与guard。
- 当前梯度曲线是S/A/B各组裁剪前范数，独立norm1裁剪；相对更新只抽每张量前16元素，名字明确sampled。视觉/语言非零梯度由工程门槛单独验证，未持续记录分项曲线。普通causal/actor/CE/native14/all32验证已有；额外前缀/关节夹爪/目标干预/记忆消融须以独立报告为准，不宣称这些诊断已完成。
- run_config永久保留原启动training_git_commit；精确恢复对配置/源码/runtime/data/world仍严格比较，只允许当前HEAD因文档等非计算内容提交而改变，并在resume_state单独记录resume_git_head。不能仅因HEAD相同而省略内容哈希，也不能借文档提交放行训练源变化。此兼容行为已加入CPU拒绝配置/源变更测试，当前八卡门槛仍需实际通过；不改指纹源码，动态身份/ETA先保存日志。没有push、部署Agilex、服务切换或机器人动作授权。

## 首轮方案收敛记录：仅Subtask回传VLM，Action-stop（2026-09-11，实施状态以上节为准）

- 用户进一步选择先让action梯度不回传VLM，只有subtask回传。首轮只设计parallel_action_stop：CE→S+B（完整参与观测计算的视觉/投影/语言），flow→A；并行分支、无生成文字回填。权威修订docs/pi05_parallel_action_stop_20260911.md覆盖下方三组/梯度配平安排。原Action-limited/full延后，不自动派发。
- 仅A读取B特征/每层prefix K/V的通路detach；S到B梯度保持，B绝非Frozen。必须测试所有action→B路径为零，CE→B视觉等真实非零。单组不额外安装原三组预留LoRA；B采用官方基座CE全量更新，未来恢复多组要重新做匹配参数化。
- 首轮普通CE和普通50×32 flow均系数1，无共享B多loss竞争，取消上一稿的0.3共享梯度校准/配平。S/B/A唯一参数所有权、各自AdamW、独立norm1裁剪。此前测量是S/A各自参数范数，不是两种loss在共享B上的范数，不能说后者悬殊已被实测证实。
- 保留official fresh、seed42、reach-arm178/20/train-only norm、5000/global256/8卡、原LR schedule，真实容量确认后定micro/accum。S保留4层512/8heads/16token及4×512递归/unroll4；S记忆不会即时传给A。仍不加query/grounding/ranking/前缀加权或共享历史。
- 此节最初只修订设计和指导文件；后续实施已由上节用户明确授权覆盖。旧队列不自动恢复；训练按工程门槛、run_concurrent及ETA规范执行。

## 最新用户纠正：共享VLM并行双分支重设计（2026-09-11）

- 用户明确原意是“VLM主线分出Subtask、Action两支；subtask loss回传VLM；action loss回传有三种模式”，随后要求重新设计实验。旧B→S→文字回填B→A、CE只到S的合同不代表用户当前目标。新实验设计以docs/pi05_parallel_multitask_redesign_20260911.md为准；历史已完成模型/数据/源码/实验结论不追溯改写。
- 本轮完成设计与指导文件更新，未实施模型、运行梯度校准/八卡gates、启动正式训练、修改提醒、部署或操作机器人。后续不要把“重新设计”或旧串联两组的派发授权当作新双分支已启动。不要自动修复恢复旧decision队列；操作前仍须读当前身份/阶段/产物。
- 首轮设计：B普通观测prefix同时供S末层特征与A各层K/V；删除S文字回填，S文字只作输出/评估。S保留4层512维8头/16token及4×512观测递归/unroll4；移除新路径的B特征no_grad/detach，使CE训练视觉/投影/语言主干。S内复用词表的查表/输出视图仍detach，防止词表捷径替代观测梯度。A仍原结构/native Piper/50×32。S记忆不即时传给A，不能宣称动作获得了历史；首轮不叠加query/grounding/ranking/前缀加权。
- 三个拟议组parallel_action_stop/limited/full：CE三组都更新完整参与路径的B；action分别不更新B、仅更新末两层LoRA16/32、更新全部参与路径。第一组不可称Frozen VLM。三组安装同样初始零输出adapter，CE更新同样base+adapter；仅action梯度掩码变化。共享B唯一optimizer所有者，分别计算/合并允许梯度后更新一次，S/B/A独立裁剪。详细loss权重的一次train-only无更新梯度校准规则见协议，不按标量loss大小拍权重、不沿用旧S与A参数范数当B共享梯度。
- 共同official fresh B/A、新seed42 S/adapter、现有reach-arm178/20/no-test/train-only norm、5000/global256/8卡和原LR schedule。micro32accum1是待真实容量测试的首测方案，不能假设CE→vision全图仍与旧配方同耗时/显存。所有GPU任务必须run_concurrent；不查询GPU占用、不停止其他任务或guard；以后派发按ETA一次跟进。
- 需要新variant/schema/provenance，旧schema7/8/9不可冒充并行结构。真实CE/action独立路由、共享参数单更新/全局batch规约、原生前向等价、4→8保存恢复、S文字/记忆不影响同次A输出等门槛尚未执行。三组只能比较action回传范围；验证CE→B收益的匹配detach消融列为后续选择，不自动加第四组。
- Learnable query仍属后续设计，旧讨论中“CE不得更新B”不再约束这一新实验族。未来query不得自行恢复S→A文字/隐状态接口；共享历史是另一个架构变量。
- 本轮之前2026-09-11 08:41只读核查：semantic_s实际完成5000；decision接续器02:05在old.cmdline()绝对路径与相对字符串相等检查处AssertionError，未运行原candidate最终gates或新两组八卡工程，旧ETA失效。此为历史核查状态，未来操作须再读当前JSON和身份，不按旧phase.json“waiting”猜测仍在运行。

## 当前授权与交付：RTC、决策/定位两组接续（2026-09-10 22:07）

- 用户已确认执行两组：①现有数据选臂/选物对比＋前25步强化；②①＋S轻量目标定位，只少量关键帧审核，A结构不改。随后提出learnable query经过B各层预测subtask，要求实施完再讨论；该议题仍是讨论，不替换当前两组、不默认授权第三组。
- Agilex RTC已正式安装，GUI“连续执行（RTC）”默认开启，默认更新间隔25；关闭回到同步。软掩码端点VJP、会话内上一动作参考、实际延迟对齐及30Hz异步执行，异常/耗尽停止。5+7项数学/线程、94项客户端无运动回归、真实CPU checkpoint、真实WebSocket调度65步和真实GPU服务RTC均通过；真实GPU请求309–324毫秒，25步为833毫秒。原8001模型服务使用同一reach-arm step005000重载；未执行机器人动作、未改驱动/Dataarm。重开GUI。未来schema9识别/loader已安装，27项无运动识别/握手回归通过；不能声称新权重已训练或部署。
- 原当前semantic_s继续到5000；原未启动semantic_s_actionrank取消。仅旧managed scheduler PID2489240/created1789039556.77被SIGSTOP；当前torchrun2508667/created1789040363.9与训练ranks未收信号。不要恢复旧调度器或重启旧队列。
- 新有限接续器scripts/run_decision_pair.py已派发并核验：初始当前PID2798978/created1789049196.26；操作前读logs/rtc_grounding_pair_20260910/launcher.process.json和dispatch_verified.json复核身份。phase为waiting_current_train_exit，基于pidfd退出事件等待，不轮询训练进度。先用run_concurrent接管原第一组final native/diagnostics，再退役已停止旧调度器，然后做新两组八卡4→8恢复/native/梯度gates，通过后依次正式训练。首个launcher仅因Python缺pidfd_open包装退出，未启动GPU任务；原证据归档attempt_01_pidfd_wrapper_missing，已加原生内核调用并用真实临时子进程退出验证。不得把此记录当训练失败或重复重启理由。
- 新输出checkpoints/pi05_piper_decision/{decision_prefix,decision_grounded}_seed42_v1，schema9 / official_pi05_recurrent_decision_v1。各5000/global256/seed42/8卡micro32accum1/unroll4/workers4，官方fresh B/A及相同共有全新递归S，limited末2层LoRA16/32，现有178/20/train-only norm不变。最终step005000优先。新增训练源独立文件；原在训source_manifest哈希逐项保持。
- 第一组：任务/审核布局/开盖臂均衡episode起点，保留完整因果流；起始及reach-object决策S加权，action前25权重2、后25权重1并归一化；分别同物换臂/同臂换物ranking最多2+2对/rank，native14前25、系数.1/margin.01，只正确生成未置空且边界稳定时启用。换物同时切全局task和subtask，避免文字矛盾捷径，不伪造反事实正确动作。
- 第二组：增加S-only任务条件高相机16×16目标热图，预测目标feature/xy作为额外S上下文token；A仍走普通生成文字，无隐藏状态注入。新assets/pi05_piper_decision_v1：32训练episode/64帧/117可见点，8验证episode/16帧/30点，80帧均视觉审核并与精确RGB缓存一致；遮挡不猜位置。每rank/update额外2训练keyframes用于.05系数定位监督，无额外optimizer update；明确额外图像预算。标签/原始数据/cache不改。S/定位CE只到S；action/ranking只到A与allowed B。
- CPU门槛已通过反事实/gating、前缀梯度、定位S-only梯度/共有初始化/因果性、真实资产/8初始strata/采样恢复/原生辅助batch。真实八卡工程尚待当前训练退出，当前仅排队完成，不能宣称工程已过或正式已开始。
- 新规范W&B视图https://wandb.ai/xiahy23-tsinghua-university/agentic-openpi-pi05-subtask?nw=qfbemtvwl95；训练/验证普通CE/全32flow保留，新两类ranking/前25/定位另名。首10步由trainer触发一次cloud/local指标和示例核验。用户2026-09-08原始消息明确“制作一个规范之后都按照规范配置wandb的显示”，此长期授权和下方350–356附近规范继续适用；新view首次自动审批拒绝后带该证据重试已批准完成，无未决许可。
- ETA：为替换队列22:00仅一次有界读取，当前2022/5000，近20步6.974秒；保留5000预算。粗估旧第一组9月11日04:15，新第一组9月11日16–18，两组全部9月12日04–06；新组合八卡耗时未实测。既有一次性heartbeat vla更新为9月11日18:00检查第一新候选/失败，按实际剩余量重估并延后同一跟进，不短周期轮询。不查询GPU占用，不干预其他任务/guard。
- 完整协议docs/pi05_rtc_decision_pair_20260910.md，query讨论docs/pi05_learnable_query_discussion_20260910.md；控制机context/rtc_grounding_20260910保留对应证据。当前B最终特征已经经过B多层；输入query的潜在收益是专用逐层聚合，不是层数自动保证准确。建议未来比较非对称attention的B内query与末层后query，保留递归记忆、生成文字A接口及现有梯度所有权，须另做等价/梯度/实机指标验证。


## 真机反馈核查与讨论范围（2026-09-10 20:24–20:38录像）

- 用户要求先解释15→25步，再核实选臂/混淆物体并给方案。本轮仅核查和文档；无新的训练启动、在训源码/数据修改、机器人动作或驱动重启。既有两组语义实验保持不可变，按已定ETA检查，不因本次诊断查询训练中间进度。
- 九段均为reach-arm limited_recurrent_seed42_v1/step_005000、SHA 2fcba5cab1f10985987d644bad84506bdddfc31c48c944c3a7f53f32440486af，视频M3叠字是历史名字。八段15步共265请求；20:38用户已经用25步并保存GUI设置，本轮未代改设置。块内33ms，块间259ms/257ms：串行推理执行导致等待，25减少等待次数，不消除单次空档。源码无保存设置时默认仍15。
- 茄子左、箱中、紫薯右四段都错误左开盖；三段S首次就left，一段先两次right但实际前15近乎静止，明显右动作直到第24/32步，第三次S改left并左臂启动。错误臂来自原始模型动作；不能只看全50预测认定执行前缀正确，亦不能据15步日志宣称25闭环已修好。
- 20:29两物在盒右，正确抓较后茄子；20:32两物前后交换后抓较后紫薯，S仍说eggplant；20:38使用25再次如此。支持位置捷径/目标实例绑定失败，但自然测试不能唯一定位内部视觉、S或A故障。现有数据角色均衡，不能说缺少茄子左的训练示范。
- 下一版讨论建议：现有reach-only标签和A接口不变，强化决策采样、两类独立动作对比（同物换臂、同臂换物）及实际前25步指标。物体反事实同时切全局任务/subtask，防止纯文字矛盾捷径；不伪造反事实正确动作。当前固定semantic_rules在reach-eggplant阶段的动作负例仅换臂，不能保证解决同臂换物。若仍失败，再讨论S目标定位分支和少量关键帧审核，不默认授权标注、隐藏S→A注入或新训练。
- 受控场景选物/选臂零错误作为建议验收目标，不承诺普遍100%，S文本正确不等于抓对。详细docs/pi05_reach_arm_robot_feedback_20260910.md；证据在Agilex logs/reach_arm_robot_feedback_20260910及控制机context/reach_arm_robot_feedback_20260910。


## 当前授权：候选交付并启动两组语义实验（2026-09-10晚）

- 正式首10步与8个rank已核验，1/10步本地与W&B共20项数值、图表一致；最后一次启动读取step49。首轮W&B异步上传未就绪只使只读checker退出，重试通过，未影响训练。startup_verified.json为完成证据。后续按ETA，不继续中间读取。
- ETA与当前跟进：第一组预计2026-09-11约03:00，两组全部预计11:00–13:00（北京时间）。当前控制机Codex app的一次性heartbeat vla已设为9月11日13:00检查两组完成/失败/最终诊断；若未完成，仅一次有界进度重估并延后同一个跟进。不得据下方历史提醒或旧PID另开队列。


- 用户确认候选已训完，要求转移到Agilex并设计、启动两组新实验，覆盖下方此前“只启动一个候选”的限制。候选limited_recurrent_seed42_v1已完成5000及诊断；最终step005000的68个部署文件在Agilex核验，SHA256 2fcba5cab1f10985987d644bad84506bdddfc31c48c944c3a7f53f32440486af。新schema7/schema8客户端94项无运动检查及1项真实WebSocket会话检查、27项源码依赖及实际CPU推理/严格身份检查通过。重开GUI。无机器人动作。
- 两组新实验为semantic_s、semantic_s_actionrank：前者自动给train标签近邻的差异token加权4；后者再加0.1系数/native14 margin0.01的文字动作ranking，最多4对/rank，仅正确生成且未dropout、标签窗口稳定时启用。负条件来自train现有近邻标签，共用噪声/时间/真实动作，不生成负动作标签、不对错误S做GT回填。普通CE/main flow和辅助损失分别记录。
- 两组均官方fresh B/A、seed42相同全新S，limited LoRA16/32、递归4×512、unroll4，现有reach-only数据178/20与train-only norm不改；各5000/global256/8卡micro32accum1，最终5000优先。A结构和推理接口不改，CE仅S、action及ranking仅A/limited B。详情docs/pi05_semantic_pair_20260910.md。
- 两组真实八卡4→8保存恢复和native/梯度/合并验证已通过，第一组19:39:24正式启动，第二组自动接续；第二组工程专用GT文字fixture强制额外4负例容量，正式配置engineering_condition_fixture必须false。控制logs/pi05_semantic_pair_20260910，新输出checkpoints/pi05_piper_semantic，schema8/official_pi05_recurrent_semantic_v1。全部GPU任务经run_concurrent，不查询GPU占用、不停止其他任务；按实际ETA一次跟进。


## 已授权启动 reach-arm 候选（2026-09-10）

- 用户现已明确要求启动“候选”训练，覆盖上节仅数据处理/未授权训练状态。只启动一个 limited B + recurrent S + reach-only actor 的候选，不启动原标签对照或其他B模式。继续官方fresh pi05 B/A、seed42全新S及零优化器/计数起点，不继承旧limited/recurrent权重。
- 正式配方5000 updates、global256、8卡micro32/accum1、unroll4、workers4；limited末两层语言块q/k/v/o和gate/up/down LoRA rank16/alpha32；递归S四个512维观测记忆。CE仅S，action仅A及limited B，100%生成文字条件和0.1置空。A结构/离散文字接口不变。使用新178/20划分和train-only norm，优先最终step005000。
- 新实现均为独立文件：scripts/train_reach_arm_candidate.py、run_reach_arm_candidate.py、check_reach_arm_candidate.py、evaluate_reach_arm_candidate.py、src/openpi/training/reach_arm_data.py和reach_arm_evaluation.py、src/openpi/policies/reach_arm_subtask_policy.py；schema7/official_pi05_recurrent_reach_arm_v1。原递归/frozen/limited模型文件保持。当前阶段先完成实际8卡4→8步保存恢复、梯度与merged native gates，再派发正式训练；不能把工程8步当正式候选。
- 控制记录logs/pi05_reach_arm_20260910；正式输出checkpoints/pi05_piper_reach_arm/limited_recurrent_seed42_v1。全部GPU工作通过run_concurrent登记，不查询GPU占用、不停止其他任务或guard。正式派发后按实际耗时给ETA，并仅按ETA跟进。没有机器人运动授权。
- 新RGB缓存.stage1_staging/piper_rgb224_reach_arm_v1已对534个复用视频的缓存内容/源逐项校验并新解码60个视频，总594；缓存原生路径587个精确样本通过。CPU入口/新配置和缺失或错误actor计错检查通过。验证增加全部episode首帧/起始一秒/reach起点和角色指标，最终额外做同图换目标、actor文字干预与memory reset离线诊断；不将这些代理指标等同真机成功。

## 最新用户范围与完成数据：仅 reach 加臂（2026-09-10）

- 用户明确要求先完成数据处理，且仅 reach the handle of the lid、reach the eggplant、reach the sweet potato 增加左右臂；其他阶段不需要冗余臂信息。这覆盖上一轮整条抓取/移动/释放链加臂的建议及此前未授权数据处理的状态。当前只处理茄子紫薯数据，其他五类保持不变。没有新的训练或机器人动作授权。
- 已生成 Datasets/eggplant_potato_reach_arm_v1，repo ID local/eggplant_potato_reach_arm_v1。198段/132939帧，396个reach片段/48148帧加 with the left/right arm，84791帧原标签不变；原12种字符串成为15种，仅三个reach各分左右。每段两处reach的state/action运动判断一致，396处均与此前角色审计一致，无猜测性填充。
- 所有198个新parquet的非subtask列和schema metadata与源表精确一致，原parquet/meta哈希未变，594段视频哈希、帧数/帧率及解码通过。新videos符号链接指向原始数据目录，迁移时必须保留该源目录或复制视频。meta/reach_arm_annotations.jsonl和reach_arm_provenance.json保留修改清单、依据、源和脚本哈希。
- 新资产 assets/pi05_piper_reach_arm_v1/eggplant_potato：seed42、按完整episode的任务/审核布局/角色分层178train+20val，无test。每种任务89train+10val；开盖臂train右90/左88、val右10/左10。norm只用119816训练帧及已有50步关节delta/绝对夹爪窗口定义。旧split和norm保持，不能把新统计用于旧ckpt。
- 真实原生训练读取链在CPU验证112个样本通过，train/val各8种组合，覆盖全部6个新reach字符串、未改阶段、边界与episode末帧；动作窗口与源一致、标签不进入Observation、target最大11 tokens且16预算足够。资产READY.json通过；新增脚本scripts/prepare_reach_arm_data.py和scripts/verify_reach_arm_data.py，报告docs/pi05_reach_arm_data_20260910.md。
- 后续训练需显式同时选择新root/repo ID/split/norm。当前没有改训练配置默认值、模型、推理或启动实验；limited+递归组合仍未实现。旧RGB缓存的split哈希与episode覆盖不同，不能直接沿用或只改哈希蒙混；需准备并验证新版本缓存。官方fresh初始化、最终5000步优先交付及既有梯度合同保持。

## 左右臂 subtask 可行性讨论（2026-09-10）

- 用户确认“改后 subtask 生成靠谱很多”指加入递归记忆的 S；记录为用户实机反馈。用户提出自动给必要的 subtask 增加 with left/right arm，并希望延续 limited 思路探索动作收益。本轮是可行性评估，尚未授权新的训练、实际数据重标注或机器人动作；“继承 limited”不默认授权继承训练好的 checkpoint，未另行指定时仍遵守官方 fresh 初始化规则。
- 新的只读筛查覆盖六类共1187段，均有14维绝对state/action。全部现有标签加 with the left/right arm 后最多13 tokens（当前茄子紫薯最多12，均含EOS），低于现有16-token上限。无需仅为臂后缀扩展S位置向量。证据 logs/arm_subtask_feasibility_20260910/trajectory_and_token_screen.json。
- 简单运动主导规则不是可靠的全阶段标注器：茄子紫薯1584个片段中953与已核实物体操作臂一致、623不确定、8冲突；198个reach-lid和198个reach-object均一致。8个冲突中7个放盖、1个释放紫薯，其夹爪变化均指向正确物体操作臂。建议联合抓取/释放事件与局部物体持有关系，不能仅看闭爪或谁动得更多。低置信度保留原标签；双臂都运动不等于both。不强制固定相反臂、全任务角色不变或阶段单调。
- 建议匹配比较 limited+递归S 的原标签对照和actor增强候选，均用新版9:1与最终5000步。维持CE只到S、action只到A及limited B，S只经离散文本影响A，不注入隐藏状态或锁死另一臂。当前递归模型构造/加载器限定frozen；组合需要真实实现和合同检查，尚未实现。评估必须区分首帧actor决策、同观测换目标、离线换actor文字的动作响应和真机成功，不能仅凭S文本准确率宣布质变。
- 详细可审阅方案 docs/pi05_arm_subtask_feasibility_20260910.md；原始数据、模型、训练和推理源码均未修改。

## 最新用户决定：优先最终 checkpoint 与 9:1 数据划分（2026-09-10）

- 用户完成了一系列真机测试，主观观察是训练到 5000 步的 checkpoint 总体优于之前按验证集分数选出的较早 checkpoint。记录为用户实机反馈，不宣称已有严格配对的成功率证明。后续默认优先交付/实测完整训练的最终 checkpoint（常规配方为 step_005000），同时保留验证指标及必要的较早候选供对照；不能再只提供验证集“最佳”候选，或让代理指标覆盖用户实机反馈。
- 后续实验改为约 90% 训练、10% 验证，取消独立测试集。必须按完整 episode 划分，固定 seed42，尽量保持任务与有证据的场景/左右臂分布；严禁按帧随机拆分造成同一轨迹泄漏。为新划分保存独立版本与 episode 清单，normalization 仅由新训练集计算。历史已完成实验的 split、统计量、checkpoint provenance 和指标不追溯改写；旧“test20 必须永久封存”的约束不再适用于新划分。
- 当前这一轮要求更新规则并诊断“箱子居中、茄子/紫薯在两侧时，偏向左臂先开盖而妨碍目标物抓取”的原因。可检查全部相关数据和真机记录；不据此自动启动新训练、重标注数据、改 action expert 或操作机器人。将训练数据的实际臂角色分布、场景布局与任务指令、左右臂/相机映射和推理输出分别核实，区分已确认事实与推测。

## 开盖选臂诊断（2026-09-10；规则仍以上一节用户决定为准）

- 全部198段训练来源数据的关节阶段运动与视频审阅：右臂持盖/左臂抓物100段，左臂持盖/右臂抓物98段；历史train158为81/77。茄子目标、箱居中且茄子左/右的训练示范分别31/33段，均选择对侧臂持盖。不能归因于左臂示范压倒性多数或没有这种布局。统计最终版 logs/arm_role_audit_20260910/dataset_arm_roles_v2.json；初版仅看抓物阶段闭爪会误计持续持盖臂，不使用v1 object-arm计数。
- 当前递归 recurrent_seed42_v1/step_003000 的有限CPU反事实诊断：原始训练起始观测ep22/ep64，各2组共同flow噪声，保持三路图像、状态、初始记忆和噪声，只切换eggplant/sweet-potato全局任务，共8次、4对。4对均未翻转开盖臂：ep22始终右，ep64始终左；全部S文字为reach the handle of the lid。动作数值会变化，不能声称完全忽略语言；此结论尚未对其他模型或广泛场景验证。无机器人运动或GPU任务。
- 代码核对全局任务从raw task到A prefix保留；模型输入含global task + generated subtask，不是prompt被丢失/截断。两任务前三个S标签相同、不含actor或目标分工，S准确率不直接检验选臂。现有A动作监督确实含正确臂信息，不能说完全没有选臂监督。
- 09:34:20、09:35:17、09:36:46三段实机记录均为茄子左/紫薯右、任务放茄子，递归模型错误左臂主导已出现在原始action提案；客户端前7维左、后7维右及相机RGB/名称映射一致，反馈新鲜且预检error_code0。很多其他试验本来应左臂持盖，不把15/16左臂开头当成15次错误。当天记录含官方Frozen/Limited/Full step_005000，递归本轮仍训练4000步、所测候选3000。
- 茄子训练开盖帧有轻度左偏11462/9064（约55.8%/44.2%），不能单独解释整体失败。原128帧动作验证只有7帧覆盖7/20 episode的起始1秒，恰好第0帧仅1个；需要独立起始选臂和任务反事实指标。归因最强的是目标到双臂分工的不可靠，具体视觉/状态捷径仍是待验证解释。
- 讨论建议：新版9:1按任务/布局/臂角色分层，均衡起始选臂阶段；必要时S增加actor/object监督并仍走离散文字条件，A架构不改。其他任务允许left/right/both等角色，不硬编码所有任务都由相反臂配合。目前未修改数据划分、标签、训练代码或启动新训练。详细报告 docs/pi05_arm_role_diagnosis_20260910.md。

## Completed recurrent-S pair and Agilex delivery (2026-09-10; supersedes active queue and ETA below)

- Both stateless_seed42_v1 and recurrent_seed42_v1 completed 4000 updates, official fresh pi05 B/A, shared fresh S seed42, frozen B. Selected candidates under checkpoints/pi05_piper_recurrent_s are stateless step_003500 and recurrent step_003000. Do not restart this queue or historical experiments.
- Both selected candidates are installed on Agilex and passed full deployment file/weight hashes, actual CPU loading/inference (finite 50x14, independent session replay, external subtask rejection) and GUI/catalog identity checks. Delivery completed 2026-09-10 09:22:11 Asia/Shanghai.18 inference project sources match. Evidence: docs/pi05_recurrent_delivery_20260910.md and logs/recurrent_delivery_20260910 on this server; robot logs/recurrent_deployment_20260909. No robot motion, driver restart, CUDA latency check or physical task success claimed.
- Validation20/792 causal sampled frames: stateless vs recurrent EM0.83333/0.89899, macroF1 0.75977/0.85899, generated native14 flow MSE0.033104/0.031180, Put-lid correct169/184 vs184/184. Neither offline arm reproduced the physical late-grasp failure, so do not declare it solved. Premature-next-label-at-previous-sample count26 vs61; this is not a manually validated unsafe-action count. Selection uses this same validation set; sealed test20 remains unopened.
- Reset-every-observation ablation reduces recurrent EM to0.35859. Continuous run state matters; reopen old GUI and start recurrent testing from the normal initial scene with one connection per run. Mid-episode cold starts/reconnect recovery, error recovery, and other task families remain unvalidated. New session/task resets memory. No hidden S state is injected into A.
- Training/final diagnostics finished by07:16. A control-host reboot left the original delivery daemon absent; recovery began08:44. A further control-host reboot changed the verified host boot time and removed the recovery process; the first CPU result survived on Agilex, its hash/GUI contract were reverified and the second transfer resumed detached. Check ready/complete evidence, not old launcher PIDs or ETAs.

- The one-shot delivery heartbeat vla was deleted after successful delivery; do not recreate it or repeat this completed check.

## Active authorized recurrent-S pair: overnight delivery (2026-09-09 22:29)

- The user authorized two experiments within about ten hours for Agilex testing around2026-09-10 08:00 Asia/Shanghai. This supersedes earlier planning-only/no-new-training wording. See docs/pi05_recurrent_pair_20260909.md.
- Formal queue is active: checkpoints/pi05_piper_recurrent_s/stateless_seed42_v1 first, then recurrent_seed42_v1. Both frozen B, official fresh pi05 B/A, identical fresh shared S seed42, no inherited optimizer/counters. Each has4000 updates/global256/world8/micro32/accum1/unroll4/workers4. Measured3.65 seconds/update makes two5000-step arms exceed the current time budget; this pair deliberately uses4000 and cosine decay to4000, retaining warmup500/peak2.5e-5/end2.5e-6/save-eval500. Do not silently restore5000 or start other B modes.
- S retains only four512D observation-driven memory tokens in the recurrent arm. Both arms share causal episode sampling and the same projected-feature decoding optimization. S generates discrete text, re-encoded by B through the existing action-prefix path; no S hidden/memory tokens are injected into A, no new A attention interface. CE trains only S including recurrence; action only A, B frozen. Existing labels/data transforms/norms remain; only original train158/val20, sealed test20 unopened.
- Five focused CPU tests passed. Both actual eight-rank4->8 checkpoint/resume gates and native policy gates passed, including equal shared S initialization,603 unchanged stored B tensors, actual CE-only-S/action-only-A gradients, finite50x14 actions, isolated sessions, repeatable reset and external-label rejection. Engineering8-step checkpoints are not formal candidates.
- Formal launcher identity and status: logs/pi05_recurrent_20260909/formal_launcher.process.json (initial PID3381096/creation1788964162.15), phase.json, formal_failure.json, engineering_passed.json, source_manifest.json and two *_complete.json. Verify current identity before any process action. All new code/config is fingerprinted and must remain unchanged during these runs. Do not duplicate this queue. Final validation-only oracle/empty text and recurrent reset ablations run through the reservation coordinator after both arms.
- An event-driven delivery daemon runs on control host robot@10.1.100.200, with identity /home/robot/workspace/VLA/context/recurrent_experiments_20260909/deployment/launcher.process.json (initial host PID64263). It watches completion via inotify/one SSH stream, transfers only selected deployment files, verifies all files/weights, and runs actual Agilex CPU inference. *_ready.json means deployment checked; the server *_complete.json alone is not robot deployment proof.
- Agilex runtime/catalog/GUI now support schema6 official_pi05_recurrent_s_v1 with arms stateless/recurrent, separate memory per WebSocket connection and new-run/task reset.86 no-motion client tests plus real temporary WebSocket session-isolation test passed. Reopen an old GUI. Port cleanup continues protecting unrelated services. No robot motion or driver restart authorized or performed.
- Dispatch estimate: pure training29227.6 seconds plus2700 seconds validation/transfer margin, expected ready2026-09-10 around07:21 Asia/Shanghai. Current-thread heartbeat automation id vla checks once at07:30; if unfinished, recompute ETA and delay that same follow-up. Do not poll fixed short intervals or revive old Windows/M3/R2 schedules. Preserve all unrelated jobs and reservation workers, no GPU-utilization queries.

## Revised subtask proposal: minimal S recurrence across task families (2026-09-09)

- Latest user correction supersedes the preceding remedy: saying release-lid was a verbal slip. Do not treat absent separate release/done labels as a proven cause or require terminal relabeling. The user wants minimal dataset processing and considers separate short/long history systems too large. The architecture must also fit the five other task families on this server.
- Audited actual Datasets metadata and task/subtask/episode/frame columns: EAI197 episodes; wipe plate/cup200 (four tasks); banana obstacle200 (optional avoidance, two sequences); food192 (the same four-label cycle repeats twice in every episode); three_object200 (12 labels,24 complete sequence variants). Evidence logs/subtask_lightweight_proposal_20260909/five_family_inventory.json. Fixed phase numbers, globally monotonic progress and blanket no-backtracking rules are inappropriate.
- Revised recommendation in docs/pi05_subtask_lightweight_plan_20260909.md: retain existing labels and B -> generated S text -> A interface, add one compact observation-driven recurrent state inside S (initial proposal four512D memory tokens), with sequential episode sampling and session-local reset. No second image-history bank, manual event table, task graph, new terminal labels, or extra completion/recovery heads in the first candidate. This is a proposal, not implemented behavior or authorization to launch training.
- Preserve CE-only-S ownership including its memory updater; action trains A and permitted B, never S. Detach B features, not new S memory. Never update persistent state from teacher-forced target-text hidden states or feed true prior labels. Memory is observation-driven and must remain isolated by inference session.
- Proposed matched stateless/recurrent comparison uses identical sequence sampling, official fresh B/A and matched fresh seed42 shared S initialization, zero inherited optimizer/counters, existing5000/global256 recipe subject to actual capacity gate. First freeze B to isolate S; do not automatically restart all three B arms or any historical queues. New dataset families need episode split manifests and train-only normalization; original eggplant sealed test remains closed.
- Evaluate full causal validation sequences, boundary delays, object/phase confusion and correct repeated cycles; include same-current-observation memory reset ablations and mid-episode cold starts. Existing labels suffice for the first comparison. Recurrent S is not proof of correct actions, cross-task zero-shot ability, recovery, or task success.

## Official trio subtask aliasing diagnosis and proposed remedy (2026-09-09)

- User reports serious visually similar phase confusion in all three candidates and requests verification plus a solution. Read docs/pi05_subtask_alias_diagnosis_20260909.md and logs/subtask_alias_audit_20260909. This turn authorizes diagnosis/planning, not a new training queue or robot actions.
- Verified Full run20260909_202319: after first Put lid,7 later requests return early reach/grasp-lid text, with105 actual published action steps. Frozen202631 has premature/stuck Put; Limited203120 jumps phases and201500 predicts sweet-potato reach15 times under an eggplant task. These unpaired trials do not quantify model rankings. Video overlays still say M3; actual schema5 checkpoint/SHA handshakes are authoritative.
- All deployed models are stateless across requests. Current14D state is encoded, but context.memory means current observation hidden tokens, not temporal memory. Generated text conditions A. S CE does not train B and action loss does not train S.
- Audited only158 train+20 val episodes:12 distinct text labels across two8-stage tasks; all178 end at Put the lid on the box. No separate release-lid, retreat, done or failure/recovery labels. A may have release actions within the coarse Put label; do not claim release was never action-supervised.
- Sparse128-frame flow-MSE selection misses the observed late-grasp failure. Use event/phase/order/object/terminal/recovery and action evaluation. Correct text alone may not fix release: sampled Full Put-conditioned actions also retained closing gripper targets. Physical ready times remain unreviewed; visual/logged phase is not success.
- Proposed direction: versioned terminal/readiness supervision, short causal visual/state history plus task-scale event state on S side, recoverable phase selection and explicit completion; adopted text must feed A before prediction. Preserve gradient ownership and official fresh initialization for any future experiment. Do not restore cancelled M3/R2 queues. Detailed plan is a proposal, not an achieved model improvement.


## GUI model switching port cleanup (2026-09-09)

- Agilex scripts/piper_gui_support.py now probes bind/listen with SO_REUSEADDR so TIME_WAIT after model shutdown does not falsely block relaunch. launch_server clears only identity-verified current-project model remnants and waits for port release.
- Model stop uses bounded SIGINT -> SIGTERM -> SIGKILL, rechecking PID/start identity, repository/script ownership and active clients before each signal. Port reuse is checked before reporting success. Unknown/unrelated port occupants are not killed; choose a free port.
- GUI reports stopping/cleaning and port released, and clears the old Popen handle after stopping. Reopen the GUI to load installed code.82 no-motion tests passed including real temporary process/socket repeated switching, residual cleanup and unrelated-service protection. No actual model service or robot action was stopped/started by this fix.
- Robot backup/evidence: logs/gui_port_fix_20260909/original and installed_tests.log/installed.json. This modifies robot GUI helpers only; no training/model sources or weights changed.


## Full official candidate deployed (2026-09-09 evening; supersedes pending Full and ETA entries)

- All three official-init seed42 arms completed5000 updates. Frozen/Limited selected step_003000; Full full_seed42_v2 selected step_001000. All three are installed on Agilex. Full SHA256 b1637560db46dd1a10e568cd6b776ef04bb2d80a92e308c09e29cff952373457;65 transferred files and all three complete weight hashes verified.
- Full actual Agilex CPU FP32 load/two fixed-noise finite50x14 checks passed;20 project inference and6 Transformers sources match.75 ROS/aloha no-motion tests and actual client websocket/GUI handshake passed. Catalog finds all three. CPU protocol check uses extended timeout; no CUDA/default5-second latency or task-success claim.
- Current hardware check FAILED: cameras fresh, both joint topics have no messages, existing drivers report stale CAN feedback. Both CAN interfaces are ERROR-ACTIVE but independent3-second passive listening received no frames on either. Do not infer readiness or bypass checks. No drivers restarted, enable/reset/actions issued.
- Existing unrelated Dataarm service on8000 preserved. Temporary CPU verification service18765 stopped. Use an available port consistently for GUI/model. Do not stop unrelated services or actuate without current authorization.
- See docs/official_gradient_robot_deployment.md. Evidence: robot logs/full_official_deploy_20260909; server logs/agilex_full_transfer_20260909. Historical Sep10 00:30 ETA is superseded by completion; Windows reminder scheduler unchanged in this deployment.


## Control workspace migration to robot@10.1.100.200 (2026-09-09)

- The user requests moving the Codex terminal/control workspace off Windows to robot@10.1.100.200:/home/robot/workspace/VLA. That host has its own SSH identity and verified direct key-based access to vla-server (surongpeng@180.184.39.60), vla-agilex (agilex@10.1.122.249), and vla-xiahongyu (xiahongyu@10.2.152.17). Do not copy/rely on Windows SSH agent forwarding or its .249-to-.181 alias mapping.
- The control workspace retains project workfiles, sanitized project conversation archives/native resume files, CONTEXT.md, and dated server/Agilex source/docs/provenance snapshots. The real repository, datasets, active training, checkpoints and GPU reservation continue on their original hosts. This server AGENTS.md remains the authoritative project guidance. Do not run old copied scratch launchers as new experiments.
- Existing full_seed42_v2 and the ETA-based single check at2026-09-10 00:30 Asia/Shanghai remain unchanged by the migration. The Windows checkpoint heartbeat is documented in the migrated context; copying that record to Linux CLI is not proof its scheduler moved. Do not duplicate training or silently replace the heartbeat with a workaround cron.


## Official Frozen/Limited candidates deployed to Agilex (2026-09-09)

- User requested migration of the two completed candidates. Both are now installed on agilex@10.1.122.249 under /home/agilex/agentic-openpi/checkpoints/pi05_piper_official_grad/{frozen,limited}_seed42_v1/step_003000. Each completed5000 training updates and selected3000 by validation. Transfer complete merged inference weights, metadata, normalization/split assets and provenance; optimizer and unmerged training files remain on the server.
- Actual SHA256 after transfer matches selected exports: frozen8c9f5624c4cfc45bcec0dab63a57672acc3116902d108a6a45fbf4326447ee80, limited322b726e6551968a10681a434ba882bd689d7844feed8feb9862c549985cd7cd; each7552992264bytes. No M3 parent or extra adapter is needed. Each actual Agilex CPU float32 load and two fixed-noise synthetic inferences passed finite50x14/repeat/input-preservation/external-subtask rejection checks, together with75 no-motion regression tests.
- Agilex now has schema5 official_pi05_backbone_v1 support in its catalog, GUI, common server and client, including frozen/limited/full modes. Strict official initialization, zero inherited updates, selected path/mode and SHA identities remain enforced.14 inference project sources and6 Transformers core runtime sources match the server. Existing hardware/reset/recording behavior remains. Reopen an old GUI to import updates. Do not repeat older claims that Agilex cannot load schema5; full_seed42_v2 weights themselves have not been transferred yet.
- Robot model server uses project .venv; GUI/client use conda aloha. See docs/official_gradient_robot_deployment.md, the robot README and logs/agilex_official_transfer_20260909/{deployment_manifest.json,frozen_cpu_load.json,limited_cpu_load.json}. Robot backup: /home/agilex/.cache/openpi-deploy/official_gradient_20260909/original. No robot action, driver restart or existing service stop was performed. These are loadability checks, not physical task-success or CUDA performance results.
- Temporary read-only SSH transfer key was limited to the two selected weight files and has been removed on Agilex and revoked from server authorized_keys. Other keys remain preserved. Full B training sources/config/checkpoints were not changed by this migration; retain its existing ETA-based completion reminder.

## Active recovery and guard coexistence (2026-09-09 11:13 Asia/Shanghai; highest priority)

- Recovery startup verified at2026-09-09 11:17 Asia/Shanghai: actual full_seed42_v2 update10 shared action/CE and separate A/B/vision/language gradient values match W&B;8 ranks alive; recipe fields match v1; all8 guards compute released at0.07043457GiB each. Evidence full_retry_v2/full_wandb_startup.json and recovery_startup_verified.json. Mean update3..10 is8.90374s under current shared workload. Estimated remaining including30-minute validation/save margin is12.84h (around2026-09-10 00:08); the existing heartbeat has been moved to a single2026-09-10 00:30 Asia/Shanghai check. Do not poll intermediate progress; the estimate can change with external workload.
- Full B v1 trained16 updates successfully, then its diagnostic subprocess failed with AttributeError: module wandb has no attribute Api. The checker called Path(.../wandb_display_env/bin/python).resolve(), dereferencing the venv symlink to the base interpreter. The controller consequently sent SIGTERM at11:03:24. This was an assistant-introduced diagnostic/runtime bug, not observed OOM, NaN or an action/B gradient failure. Actual v1 step10 action/CE/A/B/vision/language metrics match cloud history when checked with the correct interpreter. Preserve its logs; it has no saved checkpoint.
- Recovery is the same full-B arm, fresh official pi05 B/A plus identical seed42 S and unchanged5000/global256/micro32/accum1/world8/data/optimizer/LR. It now writes checkpoints/pi05_piper_official_grad/full_seed42_v2. Frozen and limited have both finished5000 updates and selected step_003000 candidates with native loading passed. Do not rerun them or increase seeds. full_seed42_v1 is preserved outside the main W&B comparison; active runs are frozen_seed42_v1, limited_seed42_v1, full_seed42_v2.
- The active_attempt.json pointer now resolves logs/pi05_official_gradient_20260909_recovery/full_retry_v2. Read that directory's sequence.process.json for the live owner; initial PID2096126/created1788923578.37. Old attempt_v2 and handoff_v1 owners have exited; their failure records are historical. No scheduler remains intentionally paused for this recovery. Never restart either old queue. Current phase/failure and full_wandb_startup.json are authoritative; dispatch is not proof startup verification passed.
- Use scripts/recover_official_full_v2.py and train_official_backbone_gradient_observed_v2.py, preserving all132 earlier fingerprinted files. The new entry only adds recovery/checker provenance before calling the unchanged observed trainer. verify_official_full_wandb_startup_v2.py uses os.path.abspath for the venv Python path, validates sys.prefix and callable wandb.Api before GPU dispatch, then checks actual update10 metrics once. Never resolve a venv Python executable symlink when spawning the environment. Reproduce integration through the exact child command, not a different shell/runtime.
- Guard activity was caused by a recognition gap: it observes registered process identities, not all experiments on the server. After our full owner exited, unregistered live RobotWin workers were ignored and holding resumed. Eight RobotWin worker identities plus their verified run_robotwin_future_token_overnight_a800.sh pipeline identity are now registered in gpu_reservation/concurrent_jobs/external_robotwin*.json. The pipeline registration covers its child transitions. They expire logically when matching PID+creation time exits; stale JSON must not count as a live lease. These leases do not grant permission to stop the jobs. Do not delete a live external lease or make holding compete with its task.
- All8 workers were verified compute_mode=released and about0.0704GiB each after registration. Keep workers/manager running; holding returns automatically only after all valid managed/external leases end. Do not force85% allocation/matmul while a protected experiment is active. Register future known external GPU pipelines by real PID+creation identity before co-running, with no fabricated always-live placeholder. This rule supersedes historical blanket holding wording where it conflicts with active-work coexistence.
- Evidence: logs/pi05_official_gradient_20260909_recovery/{incident.json,venv_root_cause_reproduced.json,failed_full_metrics_cloud_verified.json,guard_released_for_external_jobs.json,external_job_leases.json,external_pipeline_lease.json}. Continue ETA-based one-shot completion checks; no routine intermediate training polling. The user's present guard diagnosis authorizes reading worker mode/buffers and job identities; do not turn it into recurring GPU utilization polling.

## Active full-B W&B observability handoff (2026-09-09; supersedes the old queue path below)

- The user requests missing B metrics to be fixed for the next full-B experiment. Existing limited/frozen action flow loss and subtask CE are already present in W&B. B has no separate loss: the existing action loss trains A plus the permitted B parameters; CE trains S only. Never invent or backfill an independent B loss.
- The current canonical queue is resolved through logs/pi05_official_gradient_20260908/active_attempt.json, now logs/pi05_official_gradient_20260909_wandb_fix/handoff_v1/sequence.process.json. The continuation supervisor is PID1673943, created1788906116.75; verify identity from JSON before operations. The old managed scheduler PID1231717 is intentionally SIGSTOP-paused; limited torchrun PID1632777 and its training ranks continue unchanged. Do not resume the old scheduler or dispatch a duplicate full experiment. The continuation waits for limited process exit, acquires the next shared reservation lease, retires the old scheduler, checks the limited candidate, then launches full with scripts/train_official_backbone_gradient_observed.py. It preserves the guard and unrelated jobs.
- Preserve the 126 original fingerprinted sources and the new continuation manifest. No current limited trainer/model/data/optimizer source was changed. A future intentional KeyboardInterrupt in the retired attempt_v2 is explained by intentional_handoff.json; interpret current failure status only via active_attempt.json. If the continuation itself fails, inspect its failure/owner state before recovery; never blindly restart either queue.
- Observability v2 retains display_schema1 and all stable loss names; adds optim/grad_norm_a, optim/grad_norm_b, optim/grad_norm_b_vision, optim/grad_norm_b_language, optim/b_tensors_with_grad and optim/b_grad_tensor_fraction. Gradients are read on rank0 after DDP reduction and before the unchanged joint A+B norm1 clipping, at optimizer update1 and every10 updates. Vision includes SigLIP and its projector; language includes remaining shared B and token embeddings. The fraction counts non-None gradient tensors, not necessarily nonzero tensors. Do not change loss weights, optimizer groups, clipping, initialization, seed, batch or data for logging.
- Full B remains fresh official pi05 B/A plus the same seed42 S,5000 updates, global256, micro32 x accumulation1 x8. It records the observed trainer/helper/continuation/startup checker in checkpoint provenance. W&B initialization errors are fatal. A finite first10-update check compares actual local losses and positive A/B/vision/language gradient norms to cloud history; after success no intermediate monitoring is added. Distinguish queued verification from actual full-run verification.
- Existing limited/frozen history cannot supply separate unmeasured B gradients; leave those historical curves absent. Shared losses remain comparable across all three. The saved workspace retains the same URL, run colors/filter and train/validation separation; its new open B-gradient section is labelled as available starting with full B. Use docs/pi05_backbone_wandb_observability.md for semantics and evidence.
- Frozen official baseline has completed5000 updates; selected step_003000 under checkpoints/pi05_piper_official_grad/frozen_seed42_v1 passed native loading. Candidate notification can now use that path. No robot success or Agilex installation is implied. The next pending candidate is limited; use an ETA-based one-shot check, not frequent polling.

## Active priority: official-initialized three-arm experiment (2026-09-08, supersedes historical entries below)

- Formal startup verified at2026-09-09 00:29 Asia/Shanghai: frozen18 updates, mean3.69636s/update, global256, initial S/A/B counters0, matching official SHA and S digest. W&B startup train losses match local records and both input/action figures plus class Table are present. Saved comparison: https://wandb.ai/xiahy23-tsinghua-university/agentic-openpi-pi05-subtask?nw=3uzlvjh6wm5 . Existing heartbeat checkpoint is updated to one2026-09-09 06:30 Asia/Shanghai check for the first candidate; current whole-sequence estimate is around20:00-21:00 that day. No intermediate formal polling; at the scheduled check notify new completed candidates and re-estimate the next pending arm using the same reminder. These are startup estimates, not guarantees. See formal_startup_and_eta.json and wandb_live_verification.json.
- All3 real CPU route/initialization and8-rank update/save/resume/native gates passed in attempt_v2/engineering_passed.json. Formal frozen_seed42_v1 was dispatched on2026-09-09 00:26 Asia/Shanghai; limited_seed42_v1 then full_seed42_v1 follow automatically in the same finite sequence. Each independently reloads official B/A and fresh seed42 S. Approximate engineering update seconds: frozen3.6575, limited3.7014, full4.9700. Do not confuse dispatch with completion; verify current identity and final per-arm candidate/load records.
- Initial engineering status: all3 CPU official-weight/route checks passed with identical fresh S digest. Attempt v1 completed frozen8-rank save/resume but stopped on an incorrect native-check stored-tensor count. Attempt v2, resolved by active_attempt.json, fixes603 stored B tensors plus the tied embedding/head alias; frozen native loading now passed. Model/trainer/data sources remain identical and completed evidence is reused only after source verification. This snapshot is historical; read current phase before dispatch/recovery.
- Current finite implementation and run identity: docs/pi05_official_gradient_experiments.md and logs/pi05_official_gradient_20260908/active_attempt.json. The runner scripts/run_official_gradient_trio.py performs CPU routes, all-arm8-GPU save/resume/native checks, then formal frozen -> limited -> full, each from official inputs. New outputs checkpoints/pi05_piper_official_grad/{frozen,limited,full}_seed42_v1; schema5/official_pi05_backbone_v1. Do not confuse the internal legacy set_stage("m3") trainability switch with initialization. Check actual phase/gate records; dispatch alone is not proof formal training started.
- The user explicitly corrected the experiment initialization and authorized deletion of this project's models created from 2026-09-06 onward on the training server. Old stage1/M3, transition/R1/R2 and M3-derived backbone experiments are cancelled; their weight/optimizer files have been deleted per logs/pi05_official_gradient_20260908/deletion_manifest.json and deletion_result.json. Retain configs/reports/source history. Official base weights, earlier models, datasets/caches and Agilex copies are outside this deletion scope. Never restart the old queues or treat their historical checkpoints as still available.
- Run exactly three seed42 arms: frozen B baseline, limited action-loss gradients into B, full action-loss gradients into B. Each independently initializes B and A directly from the audited official pi05_base weights and initializes S from the same fresh seed42 state. No C0/M1/M2/M3/R1/R2 or other locally trained checkpoint may initialize these experiments. Reset all optimizer states and training counters to zero. Resuming a run's own checkpoint is allowed only for recovery with its exact provenance.
- For subsequent new model experiments, official pi05 initialization is the default and required starting point unless the user explicitly names a different trained parent. A previous assistant-written AGENTS.md rule is not evidence that the user authorized a trained parent. The former claim that the user authorized M3 initialization was incorrect and is superseded.
- Official local input: checkpoints/pi05_base_pytorch/model.safetensors, audited SHA256 be5b2233cf302a8fd7097e239e0e2c4680fc6f3696a55084983f2225e2d7044e. Provenance: logs/pi05_subtask_stage1/official_base_weight_audit.json. 811 JAX-derived tensors match official conversion exactly; the unused constructor-only expert LM head is the documented exception. Do not claim that unused head has an official JAX source.
- All arms share Pi0Config(pi05=True), eggplant-potato native Piper data, immutable split/train-only normalization, identical fresh S weights, loss/conditioning recipe, global batch256,5000 optimizer updates, save/eval500, seed42, cosine warmup500/peak2.5e-5/decay5000/end2.5e-6. Preserve the verified RGB224 cache, microbatch32 x accumulation1 x8 and workers4/rank unless measured failure requires a documented change applied consistently. Only B gradient ownership differs: frozen0, limited last-two-language-layer LoRA r16/alpha32, full shared vision/language B.
- CE updates S only. Generated discrete subtask conditions action prediction; action loss updates A plus only the permitted B parameters and never S. All arms share 100% generated conditions and independent0.1 empty-condition dropout, no extra warmup stage or uncounted pretraining. Teacher forcing for S's CE is separate from the generated action condition.
- Maintain the GPU reservation supervisor, protect unrelated tasks, and retain the user's ETA-based single-check notification cadence and standardized W&B display. Run arms sequentially on all8 GPUs with finite orchestration; do not create extra seeds or restore obsolete experiment queues.

## Repository and branches

- Repository: `/media/raid/workspace/surongpeng/ws_lixing/agentic-openpi`
- Server SSH account: `surongpeng@180.184.39.60` (key-based authentication; do not store or request a password).
- The active model-development branch is `upstream-openpi-main`, based on Physical Intelligence OpenPI upstream `main` at `215abfb217dbac7d5f1273282331b9b1866c0479`.
- `upstream` is `git@github.com:Physical-Intelligence/openpi.git`; `origin` is `git@github.com:LixingLiu314/agentic-openpi.git`.
- The Git executable is `/media/raid/workspace/surongpeng/anaconda3/bin/git`; use it explicitly in non-interactive SSH sessions because `git` is not on the default PATH.
- The previous `lixing` working-tree edits to `pyproject.toml` and `uv.lock` are preserved as `stash@{0}` named `codex-pre-switch-upstream-openpi-main-20260907`. Do not drop it without explicit approval.
- The root worktree intentionally contains untracked datasets, checkpoints, videos, and large artifacts. Preserve them unless an explicit request authorizes their removal.

## Eggplant–potato gripper dataset

- Active dataset: `/media/raid/workspace/surongpeng/ws_lixing/agentic-openpi/Datasets/eggplant_potato_gripper_binary`.
- Restored and verified on 2026-09-07: 198 parquet episodes and 594 MP4 videos (198 each for `cam_high`, `cam_left_wrist`, and `cam_right_wrist`).
- The source backup is `robot@10.1.119.68:/media/robot/de4b4249-95b6-4a46-8aff-c981e9defe7b/foreact/datasets/eggplant_potato_gripper_binary`; use the `robot` SSH account. The source is about 2.4 GB.
- A rollback copy of the pre-restore data components and former broken `videos`/`data_quality` links is retained at `/media/raid/workspace/surongpeng/ws_lixing/agentic-openpi/Datasets/eggplant_potato_gripper_binary_prebackup_20260907`.
- Preserve `trajectory_data/` when replacing or updating the active dataset's data components.

## Pi05 subtask stage one

- Continue the sequential implementation documented in `docs/pi05_subtask_stage1_plan.md`; factual status, commands, artifacts and pending gates are in `docs/pi05_subtask_stage1_progress.md`. The design document is the original proposal; the progress document records implementation.
- Work only from the current official `upstream-openpi-main` branch. Do not reuse previous experimental branch implementations or alter the preserved stash.
- The user confirmed that generated subtask text must condition action prediction. Keep subtask targets separate from deployment observations. The hierarchy design freezes the VLM and assigns subtask and action losses disjoint trainable parameters.
- Use the immutable episode split and training-only statistics in `assets/pi05_piper_stage1/eggplant_potato`. Never silently regenerate assets for an existing experiment. Piper uses native numerical conventions, 12 joint deltas and absolute grippers; do not apply Trossen gripper or sign transforms.
- The installed LeRobot version needs `SplitLeRobotDataset` for sparse episode IDs and `video_backend="pyav"` in this environment. Preserve the endpoint-window regression checks.
- C0 is complete and selected at `checkpoints/pi05_piper_stage1/m0_pilot_seed42/step_001000` (weight SHA256 `c108e69fd72c0c2030b6c4d7e254f16d380719b661616e10f6cd0e0bee7c387f`). Fixed 128 validation frames / 2 draws yield normalized native14 flow MSE 0.03823597. Native action evaluation is in `logs/pi05_subtask_stage1/c0_native_eval_val/report.json`; gripper outputs exceed the demonstrated range frequently, and excursion magnitudes/controller units still need verification before physical deployment. Do not claim offline errors are task success rates.
- M1-M3 trainer, disjoint optimizers, per-rank stage transitions/resume, and label-free native policy are implemented. CPU real-model M1/M2/M3 and two-rank exact-resume checks passed. Eight-GPU 32-frame M1 overfit completed 500 updates with full autoregressive exact match/macro-F1 100% from step 250 onward, no unknown/truncated output, and all 812 C0 tensors unchanged. This is an engineering gate, not generalization evidence.
- Formal M1 completed 2000 updates in `checkpoints/pi05_piper_stage1/m1_pilot_seed42`; selected `step_001900` by fixed-validation macro-F1. Dense validation on 13,515 frames: exact match 0.83833, macro-F1 0.78515; black-image intervention exact match 0.11136, task+elapsed-time diagnostic exact match 0.71720. Boundary delay/jitter remains significant; see `m1_dense_val/report.json`. Check current process identity, logs and best.json before taking the next step. Formal M2 completed 500 updates at `m2_pilot_seed42/step_000500` (generated validation flow MSE 0.03580473; frozen B unchanged). Formal M3 completed and selected `m3_pilot_seed42/step_003500`: generated validation flow MSE 0.03234650, cumulative A4000/S5900, B604 tensors unchanged. Full M3 offline evaluation is active. Matched B1/O controls, final action interventions/full policy GPU latency and three-seed experiments remain pending. Do not initialize research runs from engineering/overfit weights.
- `scripts/train_subtask_hierarchy.py` recorded and archived 25 project Python sources and nine runtime modules plus runtime versions for M1. M1 is complete and its archived sources preserve its original identity. Current M2/M3 also fingerprint `training/subtask_curriculum.py` (26 project sources); do not modify recorded sources during their exact-resume checks or active formal runs. Additional evaluation/control implementations use new files. Stage transitions carry existing branch optimizer states and restart stage LR schedules. Normal deployment requires a research M3 checkpoint; oracle labels belong only to offline evaluation.
- M0 uses `MixedPrecisionZeroAdamW` with separate FP32/BF16 ZeRO instances. Save optimizer and RNG directly per rank (`training_rank_000.pt` through `training_rank_007.pt`) and atomically publish only after every shard is written. Exact resume requires unchanged world size, configuration, source and dataset fingerprints. Avoid editing source files recorded in a running experiment until it no longer needs exact resume; implement hierarchy training in a separate script. Preserve failed smoke logs.
- The user explicitly authorized stopping all current GPU tasks on 2026-09-07 and prefers training on all eight A800 GPUs. The identified Ray/RLinf launcher and descendants were stopped after identity checks; see `logs/pi05_subtask_stage1/authorized_gpu_takeover_result.json`. The earlier approval-review concern about disrupting unauthorized jobs was resolved by this authorization. Do not stop unrelated future jobs without establishing scope.
- The user requires these eight GPUs to remain reserved whenever our training finishes or fails. Keep `scripts/gpu_reservation.py` manager/workers running across jobs. Launch every GPU job through `gpu_reservation.py run --log <unique-log> -- <command>`; it releases large reservations for training and automatically restores 85% VRAM reservation and continuous GPU compute after exit, retaining a small lease during the transition. Control/status files are in `logs/pi05_subtask_stage1/gpu_reservation`. Verify all eight worker heartbeats and reservation restoration; never end our work by leaving the GPUs unreserved. The manager handles worker restarts but does not survive a server reboot automatically.
- The user requested W&B progress/loss logging, first-step input/output visualization, and measured GPU throughput improvements on 2026-09-07. Preserve local logs as the durable source; `scripts/sync_subtask_wandb.py --watch-root checkpoints/pi05_piper_stage1` uploads them to `agentic-openpi-pi05-subtask` using the existing authenticated account. Preserve failed smoke logs and record any changes to model/data/code provenance when starting a new run. Do not edit source fingerprints or change batch/workers for an exact resume.

- GPU holding requirement clarified by the user: **both GPU compute utilization and VRAM occupancy must be >=80% on every GPU**. Revision 2 of `scripts/gpu_reservation.py` continuously multiplies 8192x8192 FP16 matrices in bounded batches during holding, targets 85% VRAM, and releases compute plus large buffers before a managed job starts. The former light heartbeat was insufficient (0% utilization); do not restore that behavior. `gpu_reservation/health.json` records driver measurements and thresholds. Runtime/process identity must be checked from current status files, not stale PIDs in prose. Upgrade provenance is in `logs/pi05_subtask_stage1/gpu_compute_upgrade_record.json`; sustained/handoff verification is being run in `gpu_reservation_compute_verification`.

- After observing a brief 74-83% utilization dip in the failure-recovery stress test, the user explicitly said the reservation does not need to be perfect and instructed resuming the model work. Keep the current revision-2 continuous-matmul guard; do not spend more time optimizing it unless it materially fails. The 60-second stable-holding check and normal handoff passed, while strict post-failure sustained verification has a preserved failure record. Do not claim that every instantaneous sample is guaranteed >=80%.

- Research M3 must inherit the terminal M2 warmup checkpoint, preserving the matched action budget. M3 best-checkpoint eligibility and normal deployment require at least 20% of accumulated action updates to have no GT conditions; never select an early curriculum checkpoint merely for lower validation error.

- The finite `scripts/continue_stage1_controls.py` sequence is registered in `logs/pi05_subtask_stage1/controls_phase_sequence.process.json`. It waits for the current M3 pipeline by process identity, then runs final M3/C0 validation, eight-GPU B1/O save/resume gates, and matched 4000-update B1/O pilots plus native evaluations. Inspect its actual process/log and outputs before launching any duplicate GPU jobs. B1/O CPU checks have passed. All GPU children use the reservation supervisor; a failed gate stops this finite sequence.

- The fixed three-seed validation protocol is `assets/pi05_piper_stage1/eggplant_potato/research_protocol_v1.json`; seeds 42/43/44 share one C0. The finite replication sequence is registered at `logs/pi05_subtask_stage1/replications_sequence.process.json`, waiting for pilot controls/evaluations and the official base audit. Inspect the real process and logs before duplicating work. Final test remains sealed until validation selection and the common execution length are fixed.
- Official base audit now runs through `scripts/audit_pi05_base_ranges.py` with generation-pinned, checksummed range downloads; current identity/log are `official_base_weight_audit_ranges.process.json` / `.log`. It preserves original sequential partial prefixes and verified objects, and invokes the unchanged official conversion/equality audit. Do not assume audit success before reading `official_base_weight_audit.json`.

- Official base provenance audit is complete: `logs/pi05_subtask_stage1/official_base_weight_audit.json` verifies 811 JAX-derived tensors exactly after official conversion/casting. The only excluded tensor is the constructor-initialized `gemma_expert.lm_head.weight`, which has no official JAX source and is unused by the verified VLA path (`unused_expert_head_behavior.json`). Do not claim all 812 stored tensors came from the JAX checkpoint. Failed v1 key-coverage logs are preserved; corrected audit source is `scripts/audit_pi05_base_weights_v2.py`.
- M3 dense validation and C0 comparable native evaluation are complete; inspect their reports. Both B1/O eight-GPU resume/freeze gates passed. Formal B1 is now active and O follows in the existing controls sequence. Final evaluators default to validation; test evaluation requires `--split test --test-protocol <sealed manifest>` and fixed selected hashes/options. No final test manifest exists yet. Original completed evaluator sources are archived in `logs/pi05_subtask_stage1/evaluation_sources_v1`.

- A final finite sequence is registered at `logs/pi05_subtask_stage1/finalization_sequence.process.json`, running `scripts/finalize_subtask_stage1.py`. It waits for all three-seed validations, profiles the selected deployment G/C0/B1 policies in isolation, fixes the common execution length and all test checkpoint/options hashes, then runs the agreed final test reports (including the fixed training-only time baseline). It has not opened test data yet. Inspect its live process/log before starting competing GPU work. Avoid modifying active finite-sequence scripts or their imported orchestration helpers while they are waiting/running.
- Model usage is documented in `docs/pi05_subtask_stage1_usage.md`. Actual decoder statuses are `ok`, `empty`, and `truncated`; there is no independent hardware timeout implementation. The deployment memory/chunk-broker runtime gate in `scripts/benchmark_subtask_policy.py` is prepared and statically checked but still pending its scheduled GPU execution.

- Matched B1 seed42 training and native validation are complete at `b1_pilot_seed42/step_004000`; the live controls sequence has moved to O seed42. `g_b1_seed42_validation_pair.json` verifies paired archives and reports single-seed episode-bootstrap intervals: all main G-minus-B1 error intervals include zero. Do not claim a stable action benefit before remaining controls/seeds and final test. Read `docs/pi05_subtask_stage1_seed42_interim.md` for the reviewable interim findings. `analyze_subtask_pilot_pair.py` is a validation-only diagnostic and does not change the sealed research protocol or training sources.

- Seed42 O and its native evaluation are complete; the original controls sequence exited successfully. Replications seed43 has completed M1 (selected step2000, dense EM0.83551609/F1 0.77302482) and M2 step500, then continues M3. Inspect real current state before relying on these snapshot numbers.
- Legacy selected C0 uses schema2 and has no `engineering_smoke` field. Use `training/research_checkpoint.py::require_research_checkpoint` at post-training research gates; it admits only the exact C0 path/hash/data identity selected in the immutable research protocol. Do not mutate old metadata or generally treat missing flags as research. Ten regression checks and real C0 report/statistics paths passed; archived pre-fix sources and process handoff are in `evaluation_sources_pre_legacy_c0_fix` and `legacy_c0_compatibility_fix.json`.
- The finalizer was paused only while waiting, repaired, and relaunched. Its canonical process record now points to PID754854/created1788778869.85; always verify identity, not this historical PID. The log is appended and the old process record is preserved. It now also generates complete validation/final Markdown reports via `scripts/write_subtask_research_report.py`, which rejects incomplete final inputs. Current validation snapshots are not final reports; test data remains unused until the existing sealing sequence.

- Normal research policy loading is now CPU-verified for C0/G/B1 via `research_policy_entrypoints_cpu.json`: actual selected weights, one real validation observation, ten flow steps, repeated fixed-noise outputs exactly equal, native 50x14 finite actions, unchanged inputs, and all 50 client slices/reset/replan metadata checks pass. G uses no engineering override and rejects external subtask input. Scheduled GPU deployment latency/memory remains pending; do not infer GPU performance from this CPU gate.
- Completion audit found an outstanding prior throughput request: `observability_data_benchmark.json` is only an eight-repeated-frame CPU decoded-cache benchmark (41.77x), not measured GPU/full-training speedup. `scripts/benchmark_subtask_throughput.py --help` passes but no GPU result exists. After the current three-seed/final evaluation GPU sequence completes, run this bounded eight-GPU benchmark through `gpu_reservation.py run`, with selected C0 checkpoint and a new output JSON. It compares microbatch2/accum2 vs4/accum1 with fixed global batch32 and resident decoded observations; label that limited scope, preserve all research run configurations, and include the actual outcome before marking the overall goal complete. No extra waiting process has been created for this step; it remains an explicit next action for this thread.

## Current user priority: robot deployment (2026-09-07)

- The user cancelled further multi-seed experiments and requested immediate physical deployment of `m3_pilot_seed42/step_003500`. Replication and finalization schedulers were stopped; preserve all partial runs, logs and immutable research protocols as history. Do not restart seeds43/44, final multi-seed evaluations, or deferred throughput benchmarks automatically. This priority supersedes earlier completion gates.
- Deployment target: `agilex@10.1.122.249`, repository `/home/agilex/agentic-openpi`. The user specified conda `aloha` for the robot client; it is Python3.8/ROS, while the existing repository `.venv` is Python3.11 with model dependencies. Use matching model sources and strict normal M3 loading. Preserve the robot old branch and local hardware configuration.
- Keep the training server GPU reservation guard running while deploying on the robot. The verified post-cancellation snapshot has all eight workers holding at ~85% VRAM and 98-99% utilization.

## Robot reset and continuous video update

- User explicitly requested a fresh physical inference run with reset restored and videos saved. Robot client now defaults to native zero-joint/.07m-gripper reset for --execute, with minimum6s minimum-jerk interpolation and.25rad/s peak joint velocity; --no-reset explicitly skips it. Readonly calls never reset. Continuous30fps recording is enabled by default alongside exact inference-input archives and rendered replay.
- Code: scripts/run_subtask_piper.py, scripts/subtask_live_video.py, scripts/render_subtask_video.py; documentation: docs/pi05_m3_robot_deployment.md. Preserve the prior trial source snapshots.
- Robot trial_reset_video_02 completed actual reset (max joint error.01490rad) and60 inference requests/900 action steps, with normal CAN at end. Continuous video52.27s, exact-input replay45.8s reside in robot logs/robot_m3_seed42_20260907/trial_reset_video_02. Subtask counts: reach handle24, put lid36; this does not establish task success. No subsequent robot action trial is queued.

## Current inference output contract (2026-09-07)

- User requested README startup instructions, real-time smooth video, and minimal per-run files. Normal Piper runs now create only `logs/inference/<timestamp>/video.mp4` and `run.json`. Preserve old trial evidence; do not restore per-query NPZ/PNG/JSONL or static-input replay as defaults.
- `scripts/serve_m3_piper.sh` starts the model with .venv; `scripts/run_m3_eggplant.sh` uses conda aloha. Root README is the current startup/reset/stop guide. Execute still defaults to native reset; --reset-only does not need the model service, and readonly never enables or publishes actions.
- Video sampling and ffmpeg encoding run separately. The monotonic30Hz timeline preserves elapsed time and reports missed slots. JSON contains query state/actions/subtask, reset, status and camera/video timestamps. Nearest-frame matching is approximate unless source camera timestamps match.
- Robot real-camera readonly verification with500ms synthetic response delay passed:12.2s/366frames,zero missed slots,max gap40.85ms. Ctrl+C and connection-error finalization passed with exactly MP4+JSON. Reset target/readonly/tracking rejection passed with simulated publishers. No new physical action trial was performed.
- The robot also has compact `logs/inference/review_reset_video_02/{video.mp4,run.json}`, preserving the original completed physical trial and byte-identical continuous video. Old artifacts remain.
- This Windows SSH config currently maps the .249 alias to .181. Use explicit `-o HostName=10.1.122.249` for the user-specified robot, retaining strict host-key verification; do not accept .181's changed key.

## Subtask completion/transition proposal (2026-09-07)

- User reports long pauses after completing a subtask and explicitly confirmed that the displayed subtask stays at the old stage during the pause. They requested analysis and a development plan to pursue in a new conversation, not training or robot actions in this turn.
- Read `docs/pi05_subtask_transition_plan.md` for the full evidence and handoff. Current M3 dense validation has93.12% away-boundary vs57.48% near-boundary EM,104 late and31 early matched transitions,3 missed, and263 unmatched changes. Existing nearest-boundary metrics do not measure stable closed-loop completion latency.
- Proposed order is D0 boundary/completion annotation and event-level diagnostics, R1 boundary-focused S-only refinement with frozen B/A, then R2 causal temporal history and completion supervision only if needed. Keep subtask/action gradient isolation. All thresholds/configurations in that document are proposed, not achieved results.
- Retain seed42-only priority and notify the user upon a deployable candidate. No new training, model change or robot action was started for this proposal. Do not resume cancelled multiseed schedulers.

- User refined transition goals: slight early handoff may be acceptable for non-critical motion stages, while reliable gripper completion must be protected. See section12 of docs/pi05_subtask_transition_plan.md. Evaluate each old-to-new transition by successor readiness, including move-to-release and reach-to-grasp prerequisites; do not classify every non-gripper-named subtask as safe to end early. Allow validated windows relative to annotation, use asymmetric event costs, and distinguish3-5 dataset frames from one~0.7s policy cycle. This is a planning refinement, not authorization to change running robot control or launch training in this turn.

- Transition plan reviewed against current code and M3 reports on 2026-09-07; section 13 now fixes D0 sidecar/protocol requirements, uncertainty and review status, timestamp-based dense/low-rate event scoring, R1 global weighted-CE semantics, and R2 no_grad/session/action-adoption details. Existing CE already normalizes per example; low-rate stability needs a later actual observation, not a held display. Current Piper displays text before action checks and writes action_step_times even in readonly runs, so physical timing requires execution evidence. This review updated documentation only; D0 reviewed labels, transition training and new robot actions have not started. Preserve the seed42-only D0 -> R1 -> conditional R2 order.

## Authorized R1 implementation and training (2026-09-07)

- User now explicitly authorizes transition implementation and training, seed fixed42. After formal training reaches50 steps, stop assistant polling and leave the finite training/evaluation job running autonomously. Do not inspect GPU occupancy/utilization. Use the existing reservation supervisor for handoff/restoration without separate GPU monitoring. Do not restart multiseed work or run robot actions.
- First run is conservative R1 boundary-only CE using original labels (zero label shift), frozen M3 B/A and a fresh S optimizer. D0 physical handoff review is still pending; all readiness fields remain null/unreviewed. This R1a can test boundary sampling while human-ready labels are unavailable; it must not claim the full D0 physical-readiness gate passed. Read docs/pi05_subtask_transition_progress.md for implementation and run identity.

- R1 conservative seed42 formal run launched via scripts/run_transition_formal.py after the v2 eight-rank exact-resume and real-policy gates passed. Identity: logs/pi05_piper_transition/r1_seed42.process.json; output: checkpoints/pi05_piper_transition/r1_seed42. It autonomously runs1000 updates, full validation each250, final action checks and policy loading. Stop assistant polling at milestone_50.json; do not resume monitoring or query GPU occupancy unless the user asks. Failed v1 deterministic-resume evidence is preserved. Formal code uses deterministic math SDPA and fixed parameter-order allreduce. Physical ready labels remain unreviewed; no robot actions are authorized by this training run.

## Piper inference feedback refactor (2026-09-08)

- User requested refactoring the current robot inference pipeline with `/home/agilex/workspace/xiahongyu/agentic-openpi` as the stable reference. Implemented on `agilex@10.1.122.249:/home/agilex/agentic-openpi`; reference checkout unchanged. See `docs/pi05_m3_pipeline_refactor_20260908.md` for evidence, implementation and validation boundaries.
- Confirmed right ROS joint5 stayed at .4116435rad with fresh timestamps while passive raw CAN reported .0739277rad; right ROS control mode was also stale. The failing reset had zero model queries. This is a driver feedback/cache discrepancy, not evidence of abnormal policy output. Controlled driver restart with `mode:=1 auto_enable:=false` restored agreement. Exact SDK cache failure mechanism remains unproven.
- Robot scripts now separate `piper_robot.py`, `piper_reset.py`, `piper_can_feedback.py`, `piper_run_log.py`, with `run_subtask_piper.py` as entry point. Passive CAN uses kernel receive timestamps independently for each feedback ID and compares raw state with ROS before/during control; never replace this with ROS timestamp checks alone. Do not automatically restart drivers or resume actions on mismatch.
- Execute confirms all12 motor enable bits; reset remains native zero joints/.07m grippers, minimum6s and peak.25rad/s, now pauses trajectory advancement at.08rad command lead and reports stalled tracking/timeouts. Keep.35rad action hard guard. Preserve checkpoint/native14/gripper metre conventions; do not copy the old banana reset/gripper defaults.
- `run_m3_eggplant.sh --check-only` performs readonly preflight and writes only run.json. Normal inference still produces just video.mp4 and run.json. Schema3 `action_step_times` records actual publications only; readonly uses `preview_step_times`. Server startup wait is bounded60s and never reconnects/replays during execution. Shell wrapper isolates client arguments from catkin/conda setup.
- 19 regression checks, real5s post-restart feedback check and actual M3/camera readonly10-query/150-step test passed. Video212frames/7.07s/zero missed slots; independent ROS observer saw zero action/enable messages and zero command publishers. Records: robot `logs/pipeline_refactor_20260908`. No new physical reset or action trial was performed. Backup: robot `/home/agilex/.cache/openpi-deploy/pipeline_refactor_20260908`.

## Simple Piper GUI (2026-09-08)

- User requested a simple GUI referencing the old `workspace/xiahongyu/agentic-openpi/scripts/start_aloha_eval_gui.sh`: images, inference outputs with reserved future fields, editable/selectable tasks, checkpoint selection and step limits. Explicitly exclude manual inference annotations.
- Implemented on robot `/home/agilex/agentic-openpi`: start with `bash scripts/start_aloha_eval_gui.sh` in the desktop session. PyQt5 uses conda aloha/ROS; code is `piper_eval_gui.py`, `piper_gui_support.py`, `piper_gui_telemetry.py`. Old GUI/reference checkout unchanged. See `docs/pi05_piper_gui.md` and root README.
- GUI displays three live RGB feeds, subtask/status/uncalibrated score/age, steps, requests, latency and phase; trajectory/subgoal/completion fields are display-only placeholders. No annotation, clicks-to-trajectory, manual subtask override or injection was added.
- Task names/prompts and checkpoint history persist in `~/.config/agentic-openpi/piper_gui.json`. GUI startup is always readonly and does not automatically enable/reset/infer. It calls the existing CLI client, retaining all CAN/reset/action guards. Checkpoint selection currently accepts complete M3 deployment directories; R1 decoder-only checkpoints are rejected instead of silently loading a different policy.
- Local model load/connect and stop run in background. Stop model verifies current project/user/PID-start identity and refuses while another project inference client exists. GUI-selected checkpoint is also enforced by the CLI `--expected-checkpoint` before enabling. Inference-mode controls are locked during runs.
- Live status uses optional nonblocking loopback UDP with per-run token; no images/action arrays or control commands are sent through it. Normal run outputs remain MP4+JSON. Stop/Esc/close cancel pending starts or interrupt only the GUI client process group, then finalize video/JSON. ffmpeg now uses its own session so group SIGINT cannot cut off MP4 finalization. Model stays available on window close.
- 17 GUI checks and19 existing pipeline checks passed. Actual Qt-button, camera and M3 readonly runs completed10 queries/150 steps with213frames/7.1s/zero missed slots; actual Stop and close-during-run finalization passed. Independent ROS observers saw zero action/enable messages. Evidence: robot `logs/gui_validation_20260908`. No physical reset/actions or live checkpoint switch was performed during this GUI task.

## GUI reset recording and execution-only inference (2026-09-08)

- Latest user request supersedes earlier readonly/reset-recording defaults: remove the GUI inference-mode dropdown and inference-without-execution feature. GUI Start always passes explicit `--execute`; CLI model inference without that flag is rejected before hardware initialization. Separate `--check-only` / `--observe-only` diagnostics remain. Opening the GUI itself still starts only image display.
- Standalone reset and automatic pre-inference reset must not be recorded. `RunLog` remains memory/GUI-telemetry only until reset succeeds and all three cameras deliver fresh post-reset frames; then create the run directory, discard reset/enable telemetry, start the inference clock/JSON writer/video. Standalone reset, reset failure and reset interruption produce no run directory, JSON or video. Preserve in-memory progress and all existing CAN/enable/reset/action guards.
- The GUI output-folder button is enabled only after `run.json` exists; successful reset without a file displays completion, not an error. Stop/close still finalize any inference recording that already started.
- Installed robot source and root README/docs updated. 49 no-motion checks passed:19 GUI,19 existing pipeline,11 recording-lifecycle checks. Three real camera feeds/layout inspected without starting inference or reset. Evidence: robot `logs/gui_reset_exclusion_20260908`; backup: `/home/agilex/.cache/openpi-deploy/gui_reset_exclusion_20260908/original`.
- Never rerun the old `logs/gui_validation_20260908/verify_gui_live.py` as a readonly test: it clicks Start, which now commands physical actions. Use mocked control I/O for lifecycle tests and camera-only layout inspection unless a real action trial is explicitly authorized for the current scene.

## R1 GUI/server loading support (2026-09-08)

- After reviewing `r1_seed42/step_001000` and its failed candidate gates, the user explicitly requested adding loading code. Robot GUI now accepts M3 and R1 decoder-delta directories; `scripts/piper_checkpoint.py` relocates the recorded training-server M3 parent path under the robot checkout. No checkpoint files or candidate reports were edited.
- Selecting an unqualified R1 in the GUI passes explicit `--allow-unqualified-r1` to the existing `serve_m3_piper.sh`/`serve_subtask_policy.py` entry and displays it as an experimental model. CLI defaults continue to require qualification. Never represent this flag as a passed gate: actual metadata records `experimental`, `candidate_gate_override`, candidate results, both checkpoint paths and weight SHA256 values. The specific step1000 still has `proxy_gates_passed=false`, `action_gate_passed=false`, and `no_qualified_candidate`.
- Robot `src/openpi/policies/subtask_transition_policy.py` strictly loads the M3 parent then replaces only the decoder. Mandatory decoder/parent SHA256, frozen B/A declaration, research-checkpoint/data identity and tensor-layout checks remain active even for experiments. GUI handshake checks both paths, both hashes, variant and experiment status. The original training-server loader/source was not modified.
- Keep existing reset exclusion, native14 control, task/step GUI settings and no-annotation contract. GUI has no readonly inference mode. Closing/reopening the GUI leaves the old model service running; switch models by stopping the current inference, Stop model, then Connect/load. An initial checkpoint-mismatch connection identifies the existing service so Stop model becomes available.
- `find_server` must use the final effective `--port` value, including `--port=VALUE`, because shell defaults precede user overrides. Never identify the process from the first default port.
- 72 regression checks passed. Actual R1 + M3 CPU FP32 loading, three deterministic warmups, WebSocket finite50x14 output and real GUI metadata/three-camera connection passed, without starting a robot client. Temporary CPU service exited; existing M3/8000 was preserved. CPU BF16 emulation was too slow, so the interrupted attempt is preserved; only explicit CPU service uses FP32. CUDA defaults unchanged.
- Evidence: robot `logs/r1_gui_loading_20260908`; backups: `/home/agilex/.cache/openpi-deploy/r1_gui_loading_20260908/original`. Restart the GUI to pick up new source. No physical R1 reset/action trial was performed or scheduled by the loading-code task.

## Current training recipe and action-to-backbone experiments (2026-09-08)

- The user explicitly authorized two seed42 training runs: limited action-loss gradients into B and full action-loss gradients into B. This supersedes the earlier mandatory frozen-B restriction **for these ablations**. Keep old M3/R1 checkpoints, protocols, sources and robot deployments intact. Do not run extra seeds or an unsolicited third control.
- For these and subsequent model experiments, align the common training recipe to the user's `pi05_aloha_obstacle_baseline` example: `Pi0Config(pi05=True)`, `num_train_steps=5000`, **global/effective** `batch_size=256`, `save_interval=500`, and `CosineDecaySchedule(warmup_steps=500, peak_lr=2.5e-5, decay_steps=5000, decay_lr=2.5e-6)`. Count steps as optimizer updates, not microbatches. The official schedule starts at `peak_lr/(warmup_steps+1)`; do not reuse the slightly different legacy stage-one warmup formula. Apply this schedule to all trainable S/A/B parameter groups unless the user specifies an experimental exception. Do not silently retain the previous S LR1e-4 / A LR1e-5 or smaller global batch.
- The example is a reference recipe, **not an instruction to change the active robot or dataset**. Keep eggplant-potato, immutable episode split and train-only normalization, three cameras `cam_high/cam_left_wrist/cam_right_wrist`, global task prompts, native Piper14 with joint-delta/absolute-gripper training, and the existing32-dimensional/50-step model interface. Do not copy `lixing/aloha_obstacle`, Trossen `adapt_to_pi`, Trossen assets or `[0,-1.5,1.5,0,0,0]` reset onto Piper. Fresh pi05 initialization retains audited official `pi05_base` provenance; explicitly specified continuation experiments inherit their named parent instead.
- Historical implementation error (cancelled; not user-authorized initialization): both old runs started from research `checkpoints/pi05_piper_stage1/m3_pilot_seed42/step_003500` weights, with fresh S and A+B optimizers and5000 additional updates. Limited B: rank16/alpha32 LoRA on attention q/k/v/o and MLP gate/up/down projections in the final two PaliGemma language blocks; original B weights and vision remain frozen. Full B: unfreeze the entire shared PaliGemma backbone, including SigLIP, vision projection, token embedding and language blocks. Parameters absent from the action computation naturally have no action gradient; do not fabricate supervision for unused heads/final outputs.
- Both runs continue subtask CE training of S. Gradient contract: `CE -> S`, `action loss -> A + enabled B`, no CE gradients into B and no action gradients into S. Generated discrete subtask text conditions A in every training example, with the same0.1 empty-condition dropout and no new GT curriculum. Both B uses share the adapted weights, so S's input features evolve even though action gradients never enter S directly.
- Training uses differentiable joint prefix/suffix attention and recomputes vision with gradients in the full mode. Changing `requires_grad` alone cannot overcome legacy `no_grad`/detached image features. Inference retains generated text and the normal cached action path. Validate real nonzero B gradients, full-mode visual gradients, preserved loss ownership, cached/joint numerical agreement, and merged LoRA deployment before formal runs.
- New implementation is isolated in `models_pytorch/backbone_gradient.py`, `scripts/train_backbone_gradient.py`, `policies/backbone_gradient_policy.py`, and `scripts/run_backbone_gradient_pair.py`. Checkpoints use `action_backbone_v1`, schema4, complete merged deployment weights plus a separate unmerged training file for LoRA resume. The loader must retain checksum, tokenizer, native data/asset and external-subtask rejection checks; do not mislabel these checkpoints as original stage-M3 weights to bypass loading rules.
- Use eight GPUs per run sequentially through the existing reservation supervisor. Formal microbatch2 x accumulation16 x world8 = global256; hardware packing may change only before a new run and must preserve the global batch and be recorded. Do not query GPU utilization/occupancy. The managed reservation was held by `ws_liyan/robotwin2-runtime` when this work began; it is a new task outside the older Ray/RLinf stop authorization. Queue behind it unless the user explicitly authorizes stopping that current task.
- Continue durable local metrics, W&B without GPU telemetry, first-update input/output visualization, and per500-step validation/checkpoints. Use validation only; do not reopen the sealed test split. Preserve seed42-only priority. A loadable experimental checkpoint is not proof of physical readiness or task success; tell the user when one is available for their robot test. This training request does not authorize new robot actions.
- Current progress, commands, process identity and validation boundaries are recorded in `docs/pi05_backbone_gradient_progress.md`. Earlier snapshots of active runs in this file are historical; consult the new progress record before duplicate launches.

## R1 failure review (2026-09-08)

- Read docs/pi05_subtask_transition_failure_review_20260908.md before describing R1 as an improvement. R1a1000 completed, but all250-step validations failed proxy safeguards; best=null, no_qualified_candidate, action gate failed. Physical readiness remains unreviewed. Keep experimental loading distinct from qualification.
- Actual robot run20260908_105241_afe075d0b3 loaded the verified R1 delta, produced reach97/put2 across99 requests, and published1470 steps. No grasp text was generated; reach-conditioned actions closed the right gripper, then put-conditioned actions reopened it. The104915 R1 run also only generated reach/put. Baseline M3 has both orderly text progression and stuck runs; these are not paired success-rate measurements.
- The old low-rate event scorer classifies6/4/6 events as missed_or_unconfirmed even for perfect annotation predictions because only one observation falls inside those successor stages. Version any correction; preserve original reports. Add observation-opportunity, wrong-order and action/phase consistency measures before reusing the gates.
- Plan section15 records the revised order: audit critical physical handoff evidence and failed/retry states, repair evaluation, then test causal history plus model-owned active-stage/completion/successor logic. Do not equate gripper closure or text EOS with physical completion. This review authorized no new robot actions or training and does not cancel or duplicate independently authorized action-to-backbone experiments. Keep seed42 and the user no-GPU-occupancy/no-post50-polling preferences.

## User-authorized concurrent training (2026-09-08, supersedes waiting-only note)

- The user explicitly forbids stopping the current RobotWin task and instead authorizes trying concurrent GPU training because its load is low. Do not stop that pipeline or its children. A single capacity snapshot showed eight A800-SXM4-80GB devices with about0.6GiB allocated each; this is a launch-time observation, not a guarantee about future RobotWin demand. Avoid ongoing GPU telemetry queries.
- Reservation revision3 preserves the revision2 holding workload and adds independent identity-checked leases in `logs/pi05_subtask_stage1/gpu_reservation/concurrent_jobs`. `gpu_reservation.py run-concurrent --log ... -- ...` is the authorized shared-job entry. It does not overwrite the existing exclusive job's `control.json`. Fallback memory/compute stay released while **either** the exclusive job or any shared lease remains live, and return only after all jobs exit. Ordinary exclusive `run` remains available for nonshared work.
- The existing manager and RobotWin job were preserved. Only the eight identity-verified project guard workers were rolled to revision3. Six CPU lease-ownership tests passed; worker adoption evidence is `logs/pi05_piper_backbone_grad/reservation_concurrent_upgrade.json`. Pre-update guard source is `.stage1_staging/gpu_reservation_pre_concurrent_20260908.py`. Do not restore workers that ignore shared leases while a concurrent experiment is active.
- Each new training rank limits its PyTorch CUDA allocator to55% of device capacity to leave space for the retained job. This does not reserve the remainder for any particular job or guarantee that arbitrary future loads fit. Keep batch256 using microbatch2/accumulation16/world8; if a compatibility gate fails, stop only our experiment and preserve its evidence.
- The pair was dispatched with `scripts/run_backbone_gradient_pair.py --concurrent`. Canonical identity is `logs/pi05_piper_backbone_grad/pair_seed42.process.json`, not a PID copied from prose. Both8-rank engineering gates run first; formal limited_seed42 then full_seed42 start automatically from original M3, never from engineering checkpoints. No extra seed is scheduled.
- CPU real-M3 checks passed for both loss routes and joint/cache parity. Limited LoRA merged export matched actions/text exactly with nonzero adapter weights. Full mode had596 B tensors with nonzero action gradients, including437 visual tensors; S received no action gradients. CPU trainer saved step1, restored optimizer/RNG/config and completed step2. These are engineering results, not training-quality evaluations.
- At the first formal step500, the finite runner checks the complete saved model through the native policy loader on CPU while GPU training continues, publishing `first_deployable.json` after success. Final validation-selected checkpoints also get `policy_load_gate.json` and `candidate.json`. A thread heartbeat named `两组梯度实验 checkpoint 通知` (automation id `checkpoint`) checks availability/failure/completion every15minutes, remains quiet on unchanged state, and must not query GPU telemetry, stop other jobs, modify active sources, or actuate the robot.

- Formal launch verified: both limited/full8-rank update/save/resume/native-policy gates passed (`pair_seed42/engineering_passed.json`). The limited5000-step formal run is active and was checked at step12 with actual global batch256; full5000 follows automatically. First-update figures are saved and W&B logging is enabled. `scripts/status_backbone_gradient_pair.py` is a read-only status/config checker without GPU telemetry. Do not duplicate the active finite sequence.
- Resource-status correction: the RobotWin exclusive control had already become `holding` at Unix1788836997.1147485 before the guard-worker upgrade/our launch; original PID4167989 was later absent. This task sent no stop signals to it. Its exit cause is unverified; `last_exit.json` still names an older smoke and is not evidence about the later pipeline. Our independent lease is now preventing fallback reservation despite exclusive control=`holding`. Thus startup gates do not establish simultaneous high-load RobotWin coexistence. Preserve the user's prohibition on stopping that task if it is restarted.
- Future serving entry: `scripts/serve_backbone_gradient_piper.py --checkpoint <complete schema4 checkpoint>`, using the strict new loader and the native Piper contract. CLI import/help checks passed; no service or robot actions were started. Robot GUI installation/recognition of `action_backbone_v1` remains a separate deployment step. Transfer merged model.safetensors, metadata.json, assets and matching sources; unmerged training_model.safetensors and rank optimizer files are unnecessary for inference.

## Active boundary-improvement exploration goal (2026-09-08)

- User explicitly requests autonomous server exploration until a substantially better boundary model is obtained; queue behind existing workloads normally. This goal is active, not achieved by implementation or dispatch. Keep seed42, no GPU occupancy inspection, no robot actions, and no intermediate formal-step polling after50. Preserve independently authorized backbone-gradient/RobotWin work.
- Read docs/pi05_subtask_transition_r2_progress.md for the current implementation, fixed gates, authoritative queue identity and remaining work. Protocol: assets/pi05_piper_transition/eggplant_potato/r2_v1/protocol.json. R2 fitting uses138 original-train episodes plus20 internal calibration episodes; the original20 val episodes remain outside updates/calibration and test remains sealed.
- New metric version separates sampling confirmation opportunities from model failures, tracks wrong order/far-stage errors and failure-inclusive delay, and still rejects R1. Substantial improvement requires+8pp boundary EM with positive paired95% CI, stable/action/non-regression guards and30% failure-inclusive low-rate delay reduction. Do not substitute easier passing checks for model quality.
- The temporal autoregressive implementation retains full current B memory, past spatial summaries, model-owned active text and a label-proxy keep/advance/reidentify head. B/A stay frozen for this branch; T/D/S must receive their intended gradients. Physical readiness is still unreviewed; do not claim a physical completion model from original-label supervision.
-13 focused tests and real CPU feature/gradient/exact-resume/schedule/trainer checks passed. Inspect native_cpu_gate.json for the independent native loading result. Formal R2 training is not yet launched; queued frozen-feature extraction uses cache_queue.process.json and immutable .stage1_staging/r2_cache_source_v1, waiting for the identity-verified backbone pair and all managed jobs. Do not edit that waiting snapshot or create a duplicate cache job.

## W&B display contract v1 (2026-09-08)

- The user requires consistent, readable W&B presentation for this and all future experiments. Follow `docs/wandb_display_standard.md`, use `src/openpi/training/wandb_standard.py` for explicit event mapping/metric definitions, and create the curated saved view with `scripts/configure_wandb_workspace.py` before handing over a new experiment. Add this logging source to a new run's provenance before launch; do not retrofit fingerprinted active/waiting training sources.
- All learning curves use explicit `trainer/step` = completed optimizer updates since this initialization. Separate `train/*` from `val/*`; never mix raw `loss_subtask` events, JSONL/W&B transport steps, native14 vs all32 loss definitions, or fractions vs percentages. No recursive numeric flattening or raw-event W&B logging. Keep local JSONL as the durable source; rank0 is the only direct writer.
- Use concise variant/seed names, stable run IDs, an experiment comparison `display_set`/group, versioned schema and complete config provenance. Default view: 7 main plots in validation/learning sections; diagnostics, performance and media/class Table are collapsed. Disable automatic panel generation, cross-run averaging, smoothing/outlier hiding and GPU/system telemetry. Keep engineering runs outside research comparisons. New metric semantics require explicit definitions/versioning, not reusing an incompatible name.
- Current immutable limited/full seed42 runs are displayed through `scripts/sync_wandb_standard.py` (deduplicated single-instance CPU/network reader, no GPU calls). Canonical display set is `backbone-grad-m3-s42`; view: https://wandb.ai/xiahy23-tsinghua-university/agentic-openpi-pi05-subtask?nw=2lxpka702pl . These `viewv1-*` / `job_type=metrics-view` runs are presentation mirrors, not additional training experiments; never average/count them alongside their original source runs. Preserve original histories and source configs.
- Read `logs/wandb_display_v1/bridge.process.json` and verify PID creation time before any restart; do not duplicate its writer. It waits only for the already authorized finite pair, adds full mode when actual source logs appear, and exits after completion or owner exit. Display dependencies live only in `.stage1_staging/wandb_display_env`; do not pip-upgrade the active training `.venv`. The saved workspace template can be reapplied idempotently; use a separate receipt for another comparison set.


## R2 finite experiment dispatch (2026-09-08)

- R2 training/evaluation queue is now registered in `logs/pi05_piper_transition/r2_exploration_seed42/training_queue.process.json`, verified waiting for the existing cache job. Formal R2 training has not started at this snapshot. Read current phase/final manifests before assuming a historical state, and do not duplicate the waiting job.
- Its immutable source snapshot is `.stage1_staging/r2_training_source_v1`, manifest SHA256 `d20d8aa89c586ad0d1ec538f57e5d363202feb8c00e62c9c66e4abf9c7dde87b`. Preserve it and the independently waiting cache snapshot. New variants/fixes must use a reviewed new snapshot after preserving previous evidence, never mutate active sources.
- Prelaunch CPU evidence: `prelaunch_cpu_checks.json`,22 focused tests, actual trainer v3 save/resume, inspected first-update figure, four-observation live/cache bit-exact history check, native action evaluator execution, and three-query reconstructed failure replay. The two-update engineering R2 and M3 both still predict final put at94/95 and are correctly rejected; this is not a formal model-quality result.
- The finite sequence waits for all managed work, then runs eight-rank exact parameter/optimizer/RNG resume and CUDA native/history gates before fresh M3-derived5000/global256/seed42 training. It uses the common recipe and reservation supervisor; it never polls formal steps. No GPU occupancy queries, robot service deployment, or physical actions are authorized by this sequence.
- Planned output is `checkpoints/pi05_piper_transition/r2_temporal_seed42_v1`. Only internal calibration selects checkpoint/keep threshold. Final original-val semantic gates, all140 boundaries plus128 natural native-action samples, and all99 approximate failure observations determine `candidate.json`. Normal loading requires exact checkpoint/hash plus passed semantic/action/native/stress flags. Inspect `experiment_result.json` for full completion; `training_complete.json` alone is insufficient. The exploration goal remains active until an actual substantially improved model passes and is reviewed.

- Additional FIT-only and failed-input audit: read `docs/pi05_transition_fit_audit_20260908.md`. Grasp stages are not generally too short for D supervision; do not shrink the uncertainty mask on that unsupported assumption. Acting hand varies across episodes, and width alone is not readiness. In tested CPU M3/R1 image/state crossovers, q94 images maintain final-put even with q70 state, whereas q70 images + q94 state produce grasp; this is a local counterfactual model diagnosis on approximate crops, not a physical-cause or R2-success claim. Existing queued cache/training snapshots were fully verified unchanged.


## Completed limited model: paired failure-text diagnostic

- The independently authorized limited backbone-gradient run has completed; its existing selector chose `checkpoints/pi05_piper_backbone_grad/limited_seed42/step_001500`, SHA256 `86ead156ecc8b236cf91b49385b72c726e7fd4e13e32f74003389186ae52b42f`. Normal native loading passed. The pair has dispatched its full run; R2 still waits behind that pair. Do not duplicate or interrupt either sequence.
- The fixed128-frame selected-checkpoint comparison gives M3 EM0.8203125 versus limited0.828125 (5 corrections,4 new errors); normalized native14 flow MSE0.0323465006 versus0.0302298395. These are sparse validation/flow results, not dense boundary or physical success evidence. Selection used flow MSE before failure replay.
- A CPU-only paired AR-text replay on the existing99 approximate failed-run observations is registered in `logs/pi05_piper_transition/r2_exploration_seed42/limited_selected_failure_text_v1.process.json`. Inspect its current PID/creation time and report before any duplicate run. It evaluates M3 then the already selected limited model; no action prediction, training, GPU inspection, or robot I/O.
- New script `scripts/audit_backbone_failure_text.py`; immutable diagnostic snapshot `.stage1_staging/limited_failure_text_source_v1`, manifest SHA256 `659dd86f95183d75ee2b1f53d15df20f7d6afef925f3b7d4eaa32b9eff4177c4`. The R2 cache/training snapshots are unchanged. Results go to `limited_selected_failure_text_v1/report.json`; `fixed_validation_comparison.json` preserves the128-frame comparison. Text-only stress results cannot qualify the boundary-improvement goal; full semantic/action/native evidence remains required.

## Limited backbone checkpoint installed on Agilex

- User requested transferring the completed model. Installed the validation-selected `limited_seed42/step_001500` at `/home/agilex/agentic-openpi/checkpoints/pi05_piper_backbone_grad/limited_seed42/step_001500`; full B still has no final candidate at this transfer. The limited training completed5000 updates and selected1500 by fixed native14 validation flow MSE0.030229839527237345.
- Complete merged B/S/A weights SHA256 `86ead156ecc8b236cf91b49385b72c726e7fd4e13e32f74003389186ae52b42f`, size7552992264 bytes, verified after transfer. Do not transfer optimizer shards or require an extra LoRA/M3 parent for this complete export. Original model/data/loader source fingerprints matched25 deployed files.
- Robot GUI/catalog, common serve_subtask_policy.py dispatch and client now support real schema4 `stage=backbone_grad`, `variant=action_backbone_v1`, limited/full modes. Keep path/mode/variant/weight SHA/parent SHA/experimental handshake checks. M3/R1 behavior, reset exclusion, native controls and MP4+JSON recording remain covered by70 no-motion checks. Restart an old GUI to load the code; do not silently relabel this model as M3 or R1.
- Actual Agilex CPU FP32 loading and two synthetic-observation fixed-noise inferences passed: finite50x14 actions, bit-exact repeat, unchanged inputs and external-subtask rejection. This is not robot success-rate or CUDA performance evidence. No existing service was stopped and no robot action was run. Evidence: robot logs/backbone_deploy_limited_1500/deployment_manifest.json and robot_cpu_load_gate.json. Read docs/backbone_limited_robot_deployment.md for startup. Backup: /home/agilex/.cache/openpi-deploy/backbone_limited_1500_20260908/original.


## Training ETA and follow-up cadence (2026-09-08)

- The user requires an estimated remaining duration first, followed by a check around that estimated completion time, instead of repeated assistant polling. This applies to future long training runs and supersedes the older every15minutes checkpoint-heartbeat instruction above.
- After launch/engineering gates, use a bounded progress read to estimate remaining optimizer updates from recent actual seconds per update. Include expected validation, checkpoint saving and final policy/evaluation gates; distinguish pure training time from the time until a usable result. Tell the user the remaining duration, an approximate absolute completion time in Asia/Shanghai, and meaningful uncertainty. Do not present an estimate as a guarantee.
- Schedule one follow-up at the estimated completion time with a reasonable buffer, updating the existing reminder rather than creating duplicates. Before then, do not repeatedly read intermediate steps/checkpoints or use fixed15-minute/other short-interval assistant or heartbeat polling. An explicit new user request for status permits a bounded read. A completion/failure event already delivered to the task may be handled immediately without adding polling to discover it.
- If training is still running at the scheduled check, read progress once, recompute the remaining time including pending gates, report the revised estimate, and reschedule the same reminder for that new estimate. Do not fall back to a tight loop. Report completion or actionable failure once and pause the completed reminder.
- This preference controls assistant status checks and notifications. Preserve the trainer's required logging, validation/checkpoint cadence, W&B metrics upload, finite job orchestration and identity-checked GPU reservation/process supervision; do not disable those systems merely to reduce assistant polling. Existing restrictions on GPU telemetry, stopping other jobs, modifying immutable active sources and robot actions remain in force.
- Current full-B estimate was sampled at 2026-09-08 22:08:45 Asia/Shanghai:555/5000 completed updates, recent100-update mean9.982745688 seconds/update,4445 remaining, about12.33 hours of pure updates. Allowing validation/save/final-load overhead and margin gives roughly13 hours to the final result (around2026-09-09 11:00, subject to workload changes). Limited B is already complete and its selected step1500 is installed on Agilex. These are historical estimates, not a reason to poll again before the scheduled follow-up.
- Existing automation `checkpoint` / `两组梯度实验 checkpoint 通知` is now a single check at 2026-09-09 11:15 Asia/Shanghai (03:15 UTC), replacing its former15-minute recurrence. Check final full-B candidate/load-gate and pair completion/failure; the already observed full step500 alone does not require an extra wake-up. If unfinished, apply the re-estimation rule above.


## User-directed larger-batch retry (2026-09-08)

- The user explicitly requests stopping all currently running project experiments and retrying a larger per-GPU batch with less accumulation. The existing backbone pair and R2 cache/training queues are stopped by identity; preserve their logs, checkpoints and snapshots. Do not independently restart those queues while this capacity/throughput retry is underway. This latest request supersedes their earlier waiting/continuation instructions. Unrelated RobotWin tasks and the GPU reservation manager/workers remain protected.
- Keep global batch256, world8 and the existing seed42/5000-step recipe. Test microbatch32/accumulation1 first through the reservation supervisor, including real backward and optimizer state allocation; reduce batch only on measured capacity or throughput evidence. Preserve old runs; use new output/provenance for changed batch packing and do not claim exact resume across a packing change.
- Retry evidence and intentional-stop receipts: logs/pi05_piper_backbone_grad/batch_retry_20260908. The previous full-B ETA reminder must be paused/replaced after the new packing is measured.

- Larger-batch gate passed: full B microbatch32 x accumulation1 x8 = global256 completed8 real updates and saved all optimizer/model state without OOM; warmed mean4.8846s/update versus historical~10s. New decoded-data pipeline passed547 exact transformed train/val samples (all178 episode starts/tails plus sampled boundaries/frames); same64-frame CPU read tests improved16.3x/17.5x, not a GPU speed claim. Use exact openpi_client PIL resizing; reject the earlier JAX-resized cache. Final cache is .stage1_staging/piper_rgb224_cache_v2, outside checkpoint assets.
- The identity-recorded retry runner is logs/pi05_piper_backbone_grad/batch_retry_20260908/retry.process.json; it performs cached eight-rank save/resume, original-vs-cached weight equality and native CUDA loading before fresh full_seed42_b32_cache_v1 training. Read docs/pi05_backbone_largebatch_retry.md. Preserve its fingerprinted sources once launched. New training is direct W&B schema v1. The completed limited model is retained; R2 queues remain stopped while this retry runs. Do not duplicate launches or restart the old full run.

- Retry identity update: the first runner intentionally stopped at an overly strict cross-independent-run weight-hash check. All2048 actual scheduled input samples/collated tensors are byte-exact between old and cached loading, and an unchanged original-loader repeat also produces non-bit-exact GPU weights (identical initial losses, tiny first-gradient differences). Do not claim bitwise cross-run GPU trajectory reproducibility. The cached run completed same-config optimizer/RNG restoration at4 and training through8. Receipts preserve both rejected hash checks and repeated-control evidence.
- Current finite restart identity is logs/pi05_piper_backbone_grad/batch_retry_20260908/verified_retry.process.json; active_attempt.json names its authoritative failure/completion files. It validates those controls, then native CUDA policy loading, before launching fresh full_seed42_b32_cache_v1. The old retry_failure.json is historical and must not be mistaken for a failure of this current attempt. Preserve the runner/extra source hashes in this identity record along with the trainer provenance. No duplicate experiment or queue restarts.

- Verified retry passed native CUDA50x14/fixed-noise/input-preservation/external-subtask checks and dispatched formal full_seed42_b32_cache_v1 from original M3 with fresh optimizers. Its batch packing is32 x1 x8, cached data workers4/rank, prefetch2, pinned/nonblocking transport, fixed validation microbatch2. Model architecture, loss routing, global256/5000/seed42/schedule remain unchanged. W&B saved view: https://wandb.ai/xiahy23-tsinghua-university/agentic-openpi-pi05-subtask?nw=e0rximjrisg . Check the current identity and actual manifests before any recovery.
- For future formal training, choose microbatch/accumulation from bounded real forward/backward/optimizer capacity and warmed throughput tests while preserving the required global batch. Do not default to microbatch2/accumulation16 just because that packing once fit. Use accumulation when actual memory or measured throughput warrants it; preserve previous runs and record changed packing as a new provenance identity. Data acceleration must preserve exact images/labels/action windows and keep large caches outside checkpoint assets.

- Formal startup confirmed: full_seed42_b32_cache_v1 updates3-7 averaged4.82248s/update (53.08 global samples/s), about2.1x the historical full-B rate. Direct W&B history matched37 actual metric values and includes the standard images/class table. The existing checkpoint reminder is now a single2026-09-09 06:00 Asia/Shanghai check, superseding the earlier11:15 ETA. Initial remaining estimate6.5-7hours; no intermediate polling. Re-estimate only at that check or on an explicit new user status request. CPU cache speedup16-17x is not an end-to-end GPU speedup; raw batch32 and cached batch32 throughput were close.
